import type { DmButtonColor, DmButtonRadius, DmButtonVariant } from '../button/button.types';

/**
 * `dm-copy-button` is a thin, opinionated wrapper over `dm-button`, so its
 * appearance API is exactly the button's — re-exported here under copy-button
 * names for a stable, self-describing public surface.
 */
export type DmCopyButtonColor = DmButtonColor;
export type DmCopyButtonVariant = DmButtonVariant;
export type DmCopyButtonRadius = DmButtonRadius;
