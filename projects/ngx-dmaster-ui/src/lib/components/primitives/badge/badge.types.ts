/** Semantic color of the badge. */
export type DmBadgeVariant = 'neutral' | 'primary' | 'success' | 'warning' | 'danger';

/** Visual treatment. */
export type DmBadgeAppearance = 'subtle' | 'solid' | 'outline';

/** Size scale. */
export type DmBadgeSize = 'sm' | 'md';
