/** Shape of the skeleton placeholder. */
export type DmSkeletonVariant = 'text' | 'rectangular' | 'circular' | 'rounded';

/** Loading animation. `none` renders a static placeholder. */
export type DmSkeletonAnimation = 'pulse' | 'wave' | 'none';

/** Border-radius scale applied by the `rounded` variant. */
export type DmSkeletonRounded = 'sm' | 'md' | 'lg' | 'full';
