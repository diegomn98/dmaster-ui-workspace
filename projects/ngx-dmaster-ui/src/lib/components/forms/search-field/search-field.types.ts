/** Semantic color, shared with the input/select palette. */
export type DmSearchFieldColor =
  'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'danger';

/** Visual variant of the field surface. */
export type DmSearchFieldVariant = 'flat' | 'bordered' | 'faded' | 'underlined';

/** Corner rounding. `md` is the family default; `full` is pill-shaped. */
export type DmSearchFieldRadius = 'none' | 'sm' | 'md' | 'lg' | 'full';
