/** Semantic color of the selected segment. */
export type DmToggleGroupColor =
  'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'danger';

/** Control height / type ramp, aligned with the rest of the field family. */
export type DmToggleGroupSize = 'sm' | 'md' | 'lg';

/** Layout direction of the segments. */
export type DmToggleGroupOrientation = 'horizontal' | 'vertical';
