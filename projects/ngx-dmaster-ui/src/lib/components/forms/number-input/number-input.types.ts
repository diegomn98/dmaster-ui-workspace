/** Semantic color, shared with the input/select/search palette. */
export type DmNumberInputColor =
  'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'danger';

/** Visual variant of the field surface (same set as `dm-search-field`). */
export type DmNumberInputVariant = 'flat' | 'bordered' | 'faded' | 'underlined';

/** Corner rounding. `md` is the field-family default; `full` is pill-shaped. */
export type DmNumberInputRadius = 'none' | 'sm' | 'md' | 'lg' | 'full';
