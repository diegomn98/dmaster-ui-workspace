import { DashboardTranslations } from './translations.types';

export const EN: DashboardTranslations = {
  shell: {
    navAria: 'Components',
    openNav: 'Open navigation',
    closeNav: 'Close navigation',
    switchToLight: 'Switch to light theme',
    switchToDark: 'Switch to dark theme',
    languageLabel: 'Language',
    githubAria: 'View source on GitHub',
    themeLabel: 'Theme',
    themeHeading: 'Pick a theme',
    searchLabel: 'Search components',
    nav: {
      intro: 'Introduction',
      home: 'Home',
      overview: 'Overview',
      gettingStarted: 'Getting Started',
      theming: 'Theming',
      componentTokens: 'Component Tokens',
      primitives: 'Primitives',
      layout: 'Layout',
      buttons: 'Buttons',
      forms: 'Forms',
      overlays: 'Overlays',
      navigation: 'Navigation',
      dataDisplay: 'Data Display',
      feedback: 'Feedback',
      skeleton: 'Skeleton',
      spinner: 'Spinner',
      badge: 'Badge',
      avatar: 'Avatar',
      icon: 'Icon',
      card: 'Card',
      accordion: 'Accordion',
      button: 'Button',
      switch: 'Switch',
      checkbox: 'Checkbox',
      formField: 'Form Field',
      searchField: 'Search Field',
      datePicker: 'Date Picker',
      errorMessage: 'Error',
      colorPicker: 'Color Picker',
      select: 'Select',
      autocomplete: 'Autocomplete',
      radioGroup: 'Radio Group',
      tabs: 'Tabs',
      table: 'Table',
      tree: 'Tree',
      timeline: 'Timeline',
      tooltip: 'Tooltip',
      dialog: 'Dialog',
      toast: 'Toast',
      divider: 'Divider',
      progress: 'Progress',
      alert: 'Alert',
      pagination: 'Pagination',
      slider: 'Slider',
      rating: 'Rating',
      breadcrumbs: 'Breadcrumbs',
      menu: 'Menu',
      popover: 'Popover',
      drawer: 'Drawer',
      kbd: 'Keyboard Key',
      command: 'Command',
      stepper: 'Stepper',
      fileUpload: 'File upload',
      numberInput: 'Number input',
      emptyState: 'Empty state',
      toggleGroup: 'Toggle group',
      buttonGroup: 'Button group',
      copyButton: 'Copy button',
      otp: 'OTP input',
    },
  },
  common: {
    importTitle: 'Import',
    usageTitle: 'Usage',
    usageDesc: 'The smallest possible snippet — this is what you write in real code.',
    playgroundTitle: 'Playground',
    playgroundDesc: 'Tweak the inputs live and copy the resulting snippet.',
    examplesTitle: 'Examples',
    apiTitle: 'API',
    defaultsTitle: 'Global defaults',
    a11yTitle: 'Accessibility',
    demos: {
      basic: 'Basic',
      variants: 'Variants',
      sizes: 'Sizes',
      states: 'States',
      appearances: 'Appearances',
      positions: 'Positions',
      withLabel: 'With label',
      forms: 'Reactive Forms',
      composition: 'Composition',
    },
  },
  shared: {
    copy: 'Copy',
    copyCode: 'Copy code',
    copied: 'Copied',
    playgroundControls: 'Playground controls',
    previewLabel: 'Preview',
    codeLabel: 'Code',
    onThisPage: 'On this page',
    api: { name: 'Name', type: 'Type', default: 'Default', description: 'Description' },
  },
  home: {
    navDocs: 'Docs',
    navComponents: 'Components',
    navRoadmap: 'Roadmap',
    navBlog: 'Blog',
    footerRights: 'MIT license © 2026 Diego Maestro',
    heroChipAngular: 'Angular 20+',
    heroChipMit: 'MIT license',
    heroLine1: 'The component library',
    heroLine2: 'Angular deserves.',
    subtitle:
      'A modern Angular 20 component library. Standalone, signals-based, with a flat, pill-radius design language — with dark mode, real theming and accessibility baked in from the very first component.',
    ctaPrimary: 'Get started',
    ctaSecondary: 'Browse components',
    copyAria: 'Copy install command',
    stats: {
      components: 'components',
      tests: 'tests, 100% passing',
      externalDeps: 'external UI dependencies',
      externalDepsValue: 'Zero',
      standalone: 'standalone, typed, OnPush',
      standaloneValue: '100%',
    },
    galleryEyebrow: 'Components',
    galleryTitle: 'A complete component kit',
    gallerySubtitle:
      'Every component below is live — themed via CSS variables, responsive, and accessible by default. Click any tile to open its docs.',
    galleryViewAll: 'View all components',
    codeEyebrow: 'Code',
    codeTitle: 'Less code, better UI.',
    codeLead:
      'Real components, real code — copy it straight into your app. No wrappers, no config, no surprises.',
    codeTabs: {
      profile: 'Profile',
      pricing: 'Pricing',
      settings: 'Settings',
    },
    themingEyebrow: 'Theming',
    themingTitle: 'Make it yours.',
    themingLead:
      'Every color is a CSS variable. Pick a palette — the whole site re-skins instantly, with no rebuild and no configuration.',
    themingHint: 'Try it: this switcher drives the real design tokens of this very page.',
    themingSwatchesAria: 'Color palettes',
    featuresAria: 'Features',
    featuresEyebrow: 'Why @dmaster/ui',
    featuresTitle: 'Built like a product, not a demo.',
    featuresLead:
      'Every component ships with the small decisions already made — states, keyboard, motion, density, dark mode.',
    features: {
      signalsTitle: 'Signals first',
      signalsBody:
        'API built with <code>input()</code>, <code>output()</code> and <code>model()</code>. <code>OnPush</code> by default and zoneless-ready.',
      zonelessTitle: 'Zoneless-ready',
      zonelessBody:
        'No zone.js anywhere. Change detection is driven by signals and <code>OnPush</code> — ready today for Angular’s zoneless future.',
      themingTitle: 'Real theming',
      themingBody:
        'CSS custom properties as the single theming source: <code>--dm-*</code> tokens, light and dark, and <code>compact · comfortable · spacious</code> densities.',
      depsTitle: 'Zero UI dependencies',
      depsBody:
        'Angular + CDK, nothing else. No icon fonts, no CSS frameworks, no runtime theme engine — a lean bundle that ages well.',
      responsiveTitle: 'Responsive by default',
      responsiveBody:
        'Mobile-first, container queries where they help, 44px touch targets and <code>prefers-reduced-motion</code> support.',
      a11yTitle: 'Accessible',
      a11yBody:
        'Semantic HTML, ARIA where needed, focus rings via <code>:focus-visible</code>, full keyboard navigation and screen-reader-tested announcements.',
    },
    startTitle: 'Install in seconds',
    startLead:
      'Add the package, import the styles and provide the library. That’s it — the full setup is under a minute.',
    startCta: 'Read the full guide',
    ctaTitle: 'Ready to build?',
    ctaLead:
      'Set up in a minute, ship the same day. Every component is production-ready, tested, and typed.',
  },
  gettingStarted: {
    lead: 'Add @dmaster/ui to your Angular 20+ app in under a minute — then unlock theming, dark mode, colors and animation, one step at a time.',
    tocTitle: 'Guide',
    stepLabel: 'Step',
    prevLabel: 'Previous',
    nextLabel: 'Next',
    sections: {
      intro: 'Introduction',
      installation: 'Installation',
      styles: 'Styles setup',
      providers: 'Provide the library',
      firstComponent: 'First component',
      darkMode: 'Dark mode',
      colors: 'Colors & tokens',
      animation: 'Animation',
      typescript: 'TypeScript',
    },
    introBody:
      '@dmaster/ui is a standalone, signals-first component library for Angular 20+. Every component is <code>OnPush</code> and zoneless-ready, themed only with CSS custom properties, and ships without any third-party UI dependencies — just Angular and the Angular CDK.',
    introHighlights: [
      '<strong>Standalone components</strong> — no modules, no barrels. Import what you use, tree-shake the rest.',
      '<strong>Zoneless-ready</strong> — signals everywhere, <code>OnPush</code> by default. Works with <code>provideZonelessChangeDetection()</code>.',
      '<strong>Themed with CSS variables</strong> — override any <code>--dm-*</code> token to re-skin without forking a single component.',
    ],
    prerequisitesTitle: 'Prerequisites',
    prerequisitesBody:
      'Before you install, make sure your project meets these baseline requirements.',
    prereqItems: [
      '<strong>Angular 20+</strong> (standalone APIs and the new signals runtime).',
      '<strong>Node 20.19+</strong> or <strong>22.12+</strong>.',
      '<code>@angular/cdk</code> and <code>@angular/forms</code> are peer dependencies (installed alongside).',
    ],
    ngAddBody:
      'The fastest way — <code>ng add</code> installs the peer dependencies (<code>@angular/cdk</code>) and wires the CDK overlay and library styles into your workspace automatically.',
    installManualTitle: 'Manual installation',
    installBody:
      'Prefer to wire it up yourself? Install the library and its peer dependencies with your favorite package manager, then follow the styles and providers steps below.',
    stylesBody:
      'Import the library stylesheet once in your global <code>styles.scss</code>. It emits the reset, design tokens and both light/dark themes.',
    stylesNote:
      'If you plan to use overlay components (<code>dm-dialog</code>, <code>dm-toast</code>, <code>dm-tooltip</code>, <code>dm-select</code>), also load the CDK structural styles once via <code>angular.json</code>.',
    providersBody:
      'Add <code>provideDmasterUI</code> in your <code>ApplicationConfig</code>. The initial <code>theme</code> and <code>density</code> stamp themselves on <code>&lt;html&gt;</code> so the CSS tokens flip automatically.',
    firstComponentBody:
      'Every component is standalone: import it directly where you use it. No modules, no barrels to remember.',
    darkModeBody:
      'Dark mode is a first-class citizen. Every color, border and shadow token has a dark counterpart — flipping happens instantly via a data attribute on <code>&lt;html&gt;</code>.',
    darkModeItems: [
      "<code>ThemeService.setTheme('light' | 'dark' | 'auto')</code> — <code>auto</code> follows <code>prefers-color-scheme</code>.",
      '<code>ThemeService.toggle()</code> — one-liner for a toggle button; use <code>resolvedTheme()</code> to read the current visible theme.',
      'Persisted in <code>localStorage</code> automatically. No flash of wrong theme on reload.',
    ],
    darkModeNote:
      'Prefer to skip the service? Set <code>data-dm-theme="dark"</code> on <code>&lt;html&gt;</code> directly — every token flips instantly.',
    colorsBody:
      'Colors are exposed as semantic CSS custom properties, mapped from the library’s own color palette. Override any of them and the whole library updates — no rebuild needed.',
    colorsItems: [
      '<strong>Six semantic colors</strong> — <code>default</code>, <code>primary</code>, <code>secondary</code>, <code>success</code>, <code>warning</code>, <code>danger</code>. Each has <code>-hover</code>, <code>-fg</code> (text on solid) and <code>-subtle</code> (flat fill) variants.',
      '<strong>Surfaces & text</strong> — <code>--dm-bg</code>, <code>--dm-bg-subtle</code>, <code>--dm-bg-muted</code>, <code>--dm-fg</code>, <code>--dm-fg-muted</code>, <code>--dm-fg-subtle</code>.',
      '<strong>Borders & shadows</strong> — <code>--dm-border</code>, <code>--dm-shadow-sm|md|lg|xl</code>. Every value tuned for both light and dark themes.',
    ],
    colorsNote:
      'Override <code>--dm-primary</code> in your <code>:root</code> and every button, focus ring and checked control re-skins in one line.',
    namedThemesTitle: 'Custom named themes',
    namedThemesBody:
      "For a full alternate palette you can switch at runtime, register a <strong>named theme</strong> with <code>provideDmasterUI({ themes })</code>, author its tokens under a <code>[data-dm-theme='…']</code> block, and apply it by name with <code>ThemeService.setTheme()</code> — light/dark stay built in.",
    animationBody:
      'Motion is centralized in a small set of duration and easing tokens — so the whole library moves at the same tempo.',
    animationItems: [
      '<strong>Durations</strong> — <code>--dm-duration-fast</code> (120ms), <code>--dm-duration-base</code> (180ms), <code>--dm-duration-slow</code> (280ms).',
      '<strong>Easings</strong> — <code>--dm-ease-out</code>, <code>--dm-ease-snappy</code> for elastic press, <code>--dm-ease-in-out</code>.',
      '<strong>Reduced motion</strong> — all duration tokens collapse to <code>0ms</code> automatically under <code>prefers-reduced-motion: reduce</code>. Zero opt-in required.',
    ],
    animationNote:
      'The button’s signature move is the elastic press: <code>scale(0.92–0.97)</code> with <code>--dm-ease-snappy</code>. It’s already built into every button.',
    typescriptBody:
      'All public types are exported from the package barrel — you don’t need deep imports. This includes shared types like <code>DmSize</code>, <code>DmTheme</code>, <code>DmDensity</code> and each component’s <code>Color</code> / <code>Variant</code> / <code>Radius</code> types.',
    whatsNextTitle: 'What’s next',
    whatsNextBody:
      'You’re ready to build. Browse the components to see live previews, playgrounds, API tables and copy-pastable snippets for each one.',
    whatsNextCta: 'Browse all components',
  },
  theming: {
    lead: 'A complete guide to customizing @dmaster/ui — from quick color overrides to fully branded themes, density scales, and motion control.',
    sections: {
      overview: 'Overview',
      semanticTokens: 'Semantic tokens',
      colorSystem: 'Color system',
      derivedCascade: 'Derived cascade',
      lightDark: 'Light & dark mode',
      namedThemes: 'Named themes',
      density: 'Density',
      motion: 'Motion & reduced motion',
      componentTokens: 'Per-component tokens',
      overlayScoping: 'Overlay scoping',
    },
    overviewBody:
      'The entire visual surface of @dmaster/ui is driven by <strong>CSS custom properties</strong>. There is no Sass to compile, no build plugin to wire, and no design-token pipeline to maintain. Override a token in plain CSS and the cascade does the rest — every component that reads that token updates instantly, in both light and dark modes.',
    overviewHighlights: [
      '<strong>Pure CSS custom properties</strong> — no Sass compilation, no build tools required. Works in any CSS file or inline <code>style</code> block.',
      '<strong>One-line color override re-derives the whole family</strong> — hover, text-on-solid, and subtle fill are computed automatically via OKLCH relative color syntax.',
      '<strong>~300 per-component tokens</strong> for granular control — radius, height, background, spacing, and more for each component.',
      '<strong>Named themes with runtime switching</strong> — register alternate palettes via <code>provideDmasterUI()</code> and switch with signals at runtime.',
    ],
    semanticBody:
      "The foundation layer is a set of ~30 semantic tokens that describe surfaces, text, borders, and shadows. They are defined once in <code>:root</code> (light theme) and redefined under <code>[data-dm-theme='dark']</code>. Every component in the library reads from these tokens — never from hardcoded colors.",
    surfaceTokens:
      '<strong>Surfaces</strong> — four elevation levels for layered layouts: <code>--dm-bg</code> (page base), <code>--dm-bg-subtle</code> (sunken areas, code blocks), <code>--dm-bg-muted</code> (input fills, hover backgrounds), and <code>--dm-bg-elevated</code> (cards, popovers, raised panels).',
    textTokens:
      '<strong>Text</strong> — three contrast levels: <code>--dm-fg</code> (primary text), <code>--dm-fg-muted</code> (secondary labels, descriptions), and <code>--dm-fg-subtle</code> (placeholders, disabled states). All maintain WCAG AA contrast ratios on their respective surfaces.',
    borderTokens:
      '<strong>Borders</strong> — <code>--dm-border</code> for standard separators and input outlines, and <code>--dm-border-strong</code> for emphasized dividers.',
    shadowTokens:
      '<strong>Shadows</strong> — <code>--dm-shadow-sm</code>, <code>--dm-shadow-md</code>, <code>--dm-shadow-lg</code>, and <code>--dm-shadow-xl</code>. Each is tuned for both themes — in dark mode shadows are deeper and more opaque to remain visible against dark surfaces.',
    colorSystemBody:
      'The library provides <strong>six semantic colors</strong>, each with five role variants. Use the base token for fills and accents, and the role variants when you need a specific contrast or surface treatment.',
    colorRoles: [
      '<strong>Base</strong> (<code>--dm-primary</code>) — solid fills, borders, icons. The reference hue for the family.',
      '<strong>Hover</strong> (<code>--dm-primary-hover</code>) — interactive hover state. Slightly darker (light theme) or lighter (dark theme) than the base.',
      '<strong>Foreground</strong> (<code>--dm-primary-fg</code>) — text that sits on top of a solid-color fill (e.g. white on a blue button). Guaranteed to pass WCAG AA.',
      '<strong>Subtle</strong> (<code>--dm-primary-subtle</code>) — tinted background for flat/light variants (badges, alerts, chip fills). Generated via <code>color-mix</code> with the base at controlled alpha.',
      '<strong>Text</strong> (<code>--dm-primary-text</code>) — text that sits on a <code>-subtle</code> background. Darkened in light mode, lightened in dark mode to maintain AA contrast.',
    ],
    derivedBody:
      'This is the key differentiator of the theming system. The <code>-hover</code> and <code>-text</code> variants are not hand-picked hex values — they are <strong>derived at render time</strong> from the base color using <strong>OKLCH relative color syntax</strong>. The formula fixes the perceptual lightness while preserving hue and scaling chroma to stay in gamut. Because lightness dominates WCAG contrast, the derived values pass AA for <em>any</em> brand color you plug in — not just the library defaults.',
    derivedNote:
      'The cascade is calibrated to <=3/255 channel drift from the original hand-tuned hex values, with WCAG contrast ratios preserved to +/-0.02. Override <code>--dm-primary</code> with any color and the hover, text, and subtle variants re-derive automatically — no rebuild, no Sass, no extra tokens to maintain.',
    lightDarkBody:
      '<code>ThemeService</code> manages the active theme as a reactive Angular signal. The theme value is stamped as a <code>data-dm-theme</code> attribute on <code>&lt;html&gt;</code>, so every CSS token flips instantly. The choice is persisted in <code>localStorage</code> and an anti-FOUC inline script in <code>index.html</code> reads it before the first paint, eliminating the flash of wrong theme on reload.',
    lightDarkItems: [
      "<code>theme()</code> — returns the raw theme value the user selected (<code>'auto'</code>, <code>'light'</code>, <code>'dark'</code>, or a named theme).",
      "<code>resolvedTheme()</code> — returns what is actually stamped on <code>&lt;html&gt;</code>. When <code>theme()</code> is <code>'auto'</code>, this resolves to <code>'light'</code> or <code>'dark'</code> based on <code>prefers-color-scheme</code>.",
      "<code>scheme()</code> — the base scheme (<code>'light'</code> or <code>'dark'</code>) of the active theme. For named themes, this comes from the <code>scheme</code> field in the theme definition.",
      '<code>toggle()</code> — flip between light and dark. Reads <code>scheme()</code> internally, so it works correctly for named themes too.',
      "<code>setTheme(name)</code> — apply any theme by name: <code>'light'</code>, <code>'dark'</code>, <code>'auto'</code>, or a custom name registered via <code>provideDmasterUI()</code>.",
      '<code>themes()</code> — returns all registered themes (built-in + custom), ideal for building a theme picker UI.',
    ],
    namedBody:
      'For fully branded alternate palettes, <strong>named themes</strong> let you register any number of custom themes and switch between them at runtime. Each named theme declares a base <code>scheme</code> (light or dark) so that <code>toggle()</code> and <code>scheme()</code> still work correctly.',
    namedSteps: [
      "<strong>1. Register the theme</strong> — pass a <code>themes</code> record to <code>provideDmasterUI()</code>. Each entry needs a <code>scheme</code> (<code>'light'</code> or <code>'dark'</code>) and a human-readable <code>label</code>.",
      "<strong>2. Author the CSS tokens</strong> — write a <code>[data-dm-theme='your-name']</code> rule block in your global stylesheet. Only override the tokens that differ from the base scheme; everything else inherits.",
      "<strong>3. Apply by name</strong> — call <code>ThemeService.setTheme('your-name')</code>. The attribute flips on <code>&lt;html&gt;</code> and CSS takes over. Use <code>ThemeService.themes()</code> to populate a picker.",
    ],
    densityBody:
      'Density controls vertical padding, gap, and height of interactive components. Three scales are available — set once via <code>provideDmasterUI({ density })</code> or change at runtime with <code>DensityService</code>. The active density is stamped as <code>data-dm-density</code> on <code>&lt;html&gt;</code> and drives <code>--dm-control-height-*</code> tokens.',
    densityScales: [
      '<strong>Compact</strong> — tighter padding and smaller heights. Ideal for data-dense views like tables and dashboards.',
      '<strong>Comfortable</strong> (default) — balanced spacing for general-purpose apps.',
      '<strong>Spacious</strong> — more generous padding and heights. Better for consumer-facing UIs and touch targets.',
    ],
    motionBody:
      'Transitions and animations read from a small set of centralized tokens. This keeps the whole library moving at the same tempo and makes it trivial to slow down, speed up, or disable motion globally.',
    motionTokens: [
      '<code>--dm-duration-fast</code> (120 ms) — micro-interactions: checkbox toggles, focus rings, button presses.',
      '<code>--dm-duration-base</code> (180 ms) — standard transitions: panel reveals, color shifts, hover states.',
      '<code>--dm-duration-slow</code> (280 ms) — larger movements: drawer slides, accordion expands, page transitions.',
      '<code>--dm-ease-out</code> — natural deceleration, used for most enter transitions.',
      '<code>--dm-ease-snappy</code> — elastic curve for the button press effect (<code>scale(0.92-0.97)</code>).',
      '<code>--dm-ease-in-out</code> — symmetric curve for looping animations and toggle states.',
    ],
    motionNote:
      "Under <code>prefers-reduced-motion: reduce</code>, all three duration tokens collapse to <code>0ms</code> automatically — zero opt-in required. Animations that don't use these tokens are disabled via the <code>reduced-motion</code> Sass mixin. The <code>ReducedMotionService</code> exposes a <code>reducedMotion()</code> signal for runtime checks.",
    componentTokensBody:
      'Beyond the global semantic tokens, every component exposes its own set of <code>--dm-&lt;component&gt;-*</code> tokens for fine-grained control. These are consumed inside the component with a verbatim fallback, so they render the default look out of the box and your override wins at any scope.',
    componentTokensItems: [
      'Tokens follow the naming convention <code>--dm-&lt;component&gt;-&lt;part&gt;-&lt;property&gt;[-&lt;state&gt;]</code> (e.g. <code>--dm-button-radius</code>, <code>--dm-switch-track-bg-checked</code>).',
      "Each component's documentation page includes a <strong>Design tokens</strong> table listing all its tokens with their defaults and descriptions.",
      "Override at any scope: globally in <code>:root</code>, per-theme in <code>[data-dm-theme='...']</code>, or per-instance on the host element. The CSS cascade controls the reach.",
    ],
    componentTokensNote:
      'These tokens are part of the public API. Renaming or removing them is a breaking change. The component SCSS never hardcodes a value without a token fallback — that is what makes theming possible without forking.',
    overlayBody:
      'Overlays (dialog, drawer, toast, tooltip, popover, select panel, menu) are portaled to the document root by the Angular CDK. This means they are <strong>outside</strong> any scoped subtree override. Global token overrides and named themes work (they are set on <code>&lt;html&gt;</code>), but a subtree-scoped override will not reach the overlay panel.',
    overlayNote:
      "To scope a theme inside an overlay, pass the CSS class via <code>panelClass</code> on <code>DmDialogConfig</code> or <code>DmDrawerConfig</code>. The class is merged with the library's structural classes and lands directly on the CDK overlay pane — so your selector reaches the panel content.",
    whatsNextTitle: "What's next",
    whatsNextBody:
      'Dive deeper into per-component tokens — every component has its own set of overridable CSS properties for precise control over radius, height, spacing, and more.',
    whatsNextCta: 'Explore component tokens',
    whatsNextCtaSecondary: 'Browse all components',
  },
  componentTokens: {
    lead: 'Every component exposes <code>--dm-&lt;component&gt;-*</code> CSS custom properties for fine-grained visual control. Override them globally, per-theme, or per-instance — no Sass, no build step.',
    totalTokensLabel: 'design tokens across all components',
    sections: {
      howItWorks: 'How it works',
      globalOverride: 'Global override',
      scopedOverride: 'Scoped override',
      perThemeOverride: 'Per-theme override',
      overlayScoping: 'Overlay scoping',
      reference: 'Token reference',
    },
    howItWorksBody:
      'Each component consumes its tokens with a <strong>verbatim fallback</strong>. The fallback is the default value the component renders out of the box. When you set the custom property at any level, the CSS cascade replaces the fallback.',
    howItWorksPattern:
      'The naming convention is <code>--dm-&lt;component&gt;-&lt;part&gt;-&lt;property&gt;[-&lt;state&gt;]</code>. For example: <code>--dm-button-radius</code>, <code>--dm-switch-track-bg-checked</code>, <code>--dm-table-row-bg-hover</code>.',
    globalBody:
      'Set a token on <code>:root</code> (or on a theme selector like <code>[data-dm-theme="midnight"]</code>) and every instance of that component inherits the override.',
    scopedBody:
      'Set a token on a subtree and only the components inside that subtree are affected. The rest of the page keeps the default.',
    perThemeBody:
      'Set tokens inside a <code>[data-dm-theme]</code> selector and they only activate when that theme is active. Combine with named themes for a fully branded override.',
    overlayBody:
      'Overlays are portaled to the document root, so subtree overrides will not reach them. Use <code>panelClass</code> on the service config (<code>DmDialogConfig</code>, <code>DmDrawerConfig</code>) to target overlay panels.',
    referenceBody:
      'All public per-component tokens, grouped by category. The <strong>Default</strong> column shows the fallback value from the component SCSS.',
    referenceCategories: {
      primitives: 'Primitives',
      layout: 'Layout',
      feedback: 'Feedback',
      buttons: 'Buttons',
      forms: 'Forms',
      navigation: 'Navigation',
      dataDisplay: 'Data Display',
      overlays: 'Overlays',
    },
    backToTheming: 'Back to Theming',
  },
  skeletonPage: {
    lead: 'Loading placeholder that mirrors the shape of the content it replaces. Zero external dependencies, accessible by default, with animations that honor <code>prefers-reduced-motion</code>.',
    demos: {
      basic: 'Basic',
      variants: 'Variants',
      animations: 'Animations',
      paragraph: 'Paragraph (count)',
      card: 'Composition: profile card',
      fluid: 'Fluid sizes',
    },
    apiCaption: 'dm-skeleton inputs',
    defaultsDesc:
      'Change the defaults of every <code>dm-skeleton</code> in the app with the <code>SKELETON_DEFAULTS</code> token or the <code>provideSkeletonDefaults</code> helper:',
    a11yItems: [
      'The host exposes <code>role="status"</code>, <code>aria-busy="true"</code> and <code>aria-live="polite"</code>: screen readers announce the loading state without interrupting.',
      'With <code>prefers-reduced-motion: reduce</code> both animations are disabled and the placeholder stays static.',
      'Accepts fluid sizes (<code>%</code>, <code>clamp()</code>, <code>vw</code>); with no explicit size it fills 100% of its container.',
    ],
    api: {
      variant: 'Shape of the placeholder.',
      width:
        'Width. Number → px; string passed through (%, rem, clamp()…). Unset, it fills 100% of the container.',
      height:
        'Height. Unset, uses the per-variant default: 1em (text), 6rem (rectangular/rounded), 2.5rem (circular).',
      animation: 'Loading animation. none renders a static placeholder.',
      rounded: 'Border-radius scale, applies with variant="rounded".',
      count:
        'Number of rendered lines/blocks (min 1). With variant="text" the last line is automatically shortened.',
    },
  },
  buttonPage: {
    lead: 'Button with a color × variant API and built-in loading, success and error states. Stable width (no layout shift), integrated spinner and screen-reader announcements via a polite live region.',
    demos: {
      colors: 'Colors',
      variants: 'Variants',
      sizes: 'Sizes',
      states: 'States',
      async: 'Simulated async action',
      iconButtons: 'Icon buttons',
    },
    apiCaption: 'dm-button inputs and outputs',
    defaultsDesc:
      'Change the defaults of every <code>dm-button</code> with the <code>BUTTON_DEFAULTS</code> token or the <code>provideButtonDefaults</code> helper:',
    a11yItems: [
      'While loading, the internal button sets <code>aria-busy="true"</code> and <code>disabled</code>, preventing duplicate submissions.',
      'State changes are announced through a visually hidden <code>aria-live="polite"</code> region using the <code>loadingLabel</code> / <code>successLabel</code> / <code>errorLabel</code> texts you provide.',
      'The label keeps its space while the spinner shows: the button never changes size (no layout shift).',
      'With <code>prefers-reduced-motion: reduce</code> the spinner stops animating and transitions become instant.',
    ],
    api: {
      color: 'Semantic color: default, primary, secondary, success, warning, danger.',
      variant: 'Visual variant: solid, flat, faded, bordered, light, ghost, shadow (colored glow).',
      radius: 'Corner rounding: none, sm, md, lg, full (pill).',
      size: 'Control size; heights follow the global density tokens.',
      loading: 'Shows the spinner and disables the button — the shortcut for a simple async state.',
      state:
        'Full state machine; success/error flash an icon. A non-idle value takes precedence over loading.',
      type: 'Native button type.',
      disabled: 'Disables the button (it is also disabled automatically while loading).',
      loadingLabel:
        'Text announced to screen readers while loading (the library ships no copy of its own).',
      successLabel: 'Text announced on success.',
      errorLabel: 'Text announced on error.',
      clicked: 'Emitted on click, only while the button is interactive (not disabled nor loading).',
    },
    asyncButtonLabel: 'Save changes',
  },
  roadmap: {
    eyebrow: 'Roadmap',
    nowLabel: 'Now',
    board: {
      backlog: 'Backlog',
      inProgress: 'In progress',
      done: 'Done',
      backlogMeta: 'planned — nothing here breaks your code',
      inProgressMeta: 'the road to 1.0',
      doneMeta: 'shipped & stable',
      targetTitle: 'A stable API',
      targetSub: 'Three things stand between here and there.',
    },
    rail: {
      foundations: 'Foundations',
      forms: 'Forms & overlays',
      catalogue: 'Full catalogue',
      current: 'Surface ramp · OTP',
      theming: 'Named themes & tokens',
      copy: 'Copy to clipboard',
    },
    focusItems: {
      freezeTitle: 'API surface freeze',
      freezeDesc:
        'Consistency pass across all {count} components — input/output names, injectable defaults, sibling parity.',
      deprecationTitle: 'Deprecation policy',
      deprecationDesc: 'One written rule for how the API evolves after 1.0.',
      docsTitle: 'Docs & case study',
      docsDesc: 'Engineering write-up + copy-paste StackBlitz templates.',
    },
    statusInProgress: 'In progress',
    statusQueued: 'Queued',
    gatesTitle: 'Quality gates',
    gates: {
      a11y: 'axe WCAG 2.1 AA · light + dark',
      visual: 'Visual regression · pinned container',
      consumer: 'Real-consumer integration build',
      provenance: 'Signed npm provenance',
    },
    next: {
      signalFormsTitle: 'Signal Forms',
      signalFormsDesc: 'First-class support; the CVA controls are forward-compatible by design.',
      tagsTitle: 'Tags input',
      tagsDesc: 'Token entry with keyboard editing and paste distribution.',
      textareaTitle: 'Autosize textarea',
      textareaDesc: 'Grows with content, sibling to the field family.',
      contextMenuTitle: 'Context menu',
      contextMenuDesc: 'Right-click / long-press, WAI-ARIA complete, on CDK.',
      scrollAreaTitle: 'Scroll-area',
      scrollAreaDesc: 'Custom, themeable scrollbars for dense panels.',
      splitterTitle: 'Splitter',
      splitterDesc: 'Resizable panes with keyboard + pointer control.',
      themeBuilderTitle: 'Theme Builder',
      themeBuilderDesc:
        'Visual editor that exports a ready-to-paste CSS block for custom named themes.',
    },
    catForms: 'Forms',
    catOverlay: 'Overlay',
    catLayout: 'Layout',
    catDx: 'DX',
    footerTitle: 'Follow the work as it lands',
    footerChangelog: 'Read the changelog',
    footerReleases: 'GitHub releases',
  },
  blog: {
    title: 'Blog',
    latestLabel: 'Latest',
    rssTitle: 'A new entry lands with every release.',
    rssDesc: 'No forms, no tracking — just the repo and an RSS feed.',
    browseCta: 'Releases on GitHub',
    rssCta: 'RSS feed',
    filterAll: 'All',
    categories: {
      a11y: 'Accessibility',
      performance: 'Performance',
      testing: 'Testing',
      architecture: 'Architecture',
      release: 'Release',
    },
    articleBack: 'All entries',
    articleWhatShipped: 'What shipped',
    articleViewRelease: 'View release on GitHub',
    articleViewChangelog: 'Full changelog',
    articleNewer: 'Newer',
    articleOlder: 'Older',
    articles: {
      v09: {
        title: 'A copy-to-clipboard button and directive',
        lead: '0.9 adds two small, focused ways to copy text: the <code>dm-copy-button</code> component and the <code>dmCopyToClipboard</code> directive. The button wraps <code>dm-button</code> and flips its icon from copy to a check as confirmation; the directive drops the same behaviour onto any button you already have. Both are SSR-safe and ship no built-in copy.',
        bullets: [
          'dm-copy-button — an icon button that copies its value and flips to a check for a moment; wraps dm-button, so it presses, focuses and themes like every other button',
          'dmCopyToClipboard directive — put it on any button you already have; exportAs gives an isCopied() signal to drive your own feedback',
          'SSR-safe clipboard — async Clipboard API through the injected Document with an execCommand fallback; (copied) / (copyError) outputs, no built-in copy',
          'Dogfooded in the docs — the Overview install command (ng add @dmaster/ui) is now copied with it',
          '45 components across 8 categories — still verified by axe (WCAG 2.1 AA) in light and dark',
        ],
      },
      v08: {
        title: 'Real theming: named themes, ~300 design tokens, and one combobox',
        lead: 'The biggest theming release yet — register named custom themes and switch them at runtime, restyle any component through ~300 public CSS tokens, and override one brand color to re-derive a whole family via OKLCH. Meanwhile dm-paginated-select folded into dm-select as an async mode. Every default render stays pixel-identical.',
        bullets: [
          "Named themes — register palettes with provideDmasterUI({ themes }), author their tokens under [data-dm-theme='name'], and switch live with ThemeService.setTheme()",
          '~300 per-component design tokens — every component exposes public --dm-<component>-* custom properties, overridable at any scope; the default render is pixel-identical',
          'Derived color tokens — override --dm-primary and its hover/text/subtle shades re-derive via OKLCH at a calibrated lightness, holding WCAG AA for arbitrary brand colors',
          'BREAKING: dm-paginated-select merged into dm-select — one combobox with an async loadFn mode (rxResource: accumulated pages, infinite scroll or load-more, debounced server search, now also with multiple)',
          'Scoped theming inside overlays — panelClass on dialog/drawer carries a subtree theme class into portaled overlays',
          'dm-select hardened — filter focus fixed, two-stage Escape, error-resilient infinite scroll, and screen-reader announcements for loading/empty states',
        ],
      },
      v07: {
        title: 'Four new components and a surface ramp with real contrast',
        lead: 'Four additions round out the catalogue — empty-state, toggle-group, button-group and an OTP input — and the dark surface ramp was re-spaced so flat fills, chips and elevated panels are actually distinguishable. Audited against WCAG AA in both themes.',
        bullets: [
          'dm-empty-state — icon + title + description + actions, custom icon slot, three sizes',
          'dm-toggle-group — a segmented control: single or multiple, radiogroup or aria-pressed semantics',
          'dm-button-group — an attached bar with 1px seams and a group-level color/variant/size cascade',
          'dm-otp — one-time-code cells with paste distribution, group separators and four surface variants',
          'Surface ramp re-spaced: the dark steps now read against each other; light "muted" is visible on white',
          'Tinted fills re-tuned so every text/fill pair clears 4.5:1 — verified by axe in CI',
        ],
      },
      v06: {
        title: 'Six components and a full WCAG AA audit, both themes',
        lead: 'The catalogue grew to 41 with stepper, rating, tree, file-upload, timeline and number-input — and the whole library went through axe-core against WCAG 2.1 A + AA, in light and dark, with every finding fixed and the suite wired into CI as a hard gate.',
        bullets: [
          'dm-stepper — linear or free wizard with per-step states and aria-current="step"',
          'dm-tree — the complete WAI-ARIA tree pattern: roving focus, full keyboard model, multi-select',
          'dm-rating, dm-file-upload, dm-timeline and dm-number-input join their families',
          'New --dm-{color}-text tokens: tinted variants keep vivid fills with AA-legible labels',
          'axe-core now runs on every PR — every route, both themes, zero violations required',
        ],
      },
      v05: {
        title: 'Autocomplete, multiple select and honest bundle numbers',
        lead: 'A free-text autocomplete joined the field family, dm-select learned multiple selection, inline filtering and option groups, dm-table gained virtual scroll — and the README started documenting the real tree-shaken cost of every component, measured with the CLI’s own pipeline.',
        bullets: [
          'dm-autocomplete — type anything, filter suggestions in an overlay, keep your text or pick one',
          'dm-select: multiple (chips), filterable, option groups and select-all — all additive; single-select untouched',
          'dm-date-picker range mode with a live hover-preview band between start and end',
          'dm-table virtual scroll: thousands of rows in a CDK viewport; sort, search and selection intact',
          'npm run size — a linker + minify pipeline that reveals the true per-component cost',
          'Docs standard raised: ~6 realistic demos per page plus a product-grade Composition recipe',
        ],
      },
      v04: {
        title: 'The field family grows: date picker, color picker, icons',
        lead: 'Two heavyweight fields — a calendar built on pure Intl with no date library, and a full color picker — plus dm-error, dm-search-field, and a three-mode icon system with a curated set on its own entry point.',
        bullets: [
          'dm-date-picker: timezone-safe calendar math, CLDR week conventions, reactive DM_DATE_LOCALE',
          'dm-color-picker: saturation/hue/alpha areas, editable hex and swatches — a CVA hex field',
          'dm-error: the mat-error equivalent; dm-form-field wires aria-invalid/aria-describedby automatically',
          'dm-search-field: leading magnifier, clear button, Escape clears, Enter submits',
          'dm-icon: Material Symbols ligatures, registered SVGs or a projected <svg> — one primitive, three modes',
          '@dmaster/ui/icons: a curated set of ~53 original outline icons, tree-shakeable',
        ],
      },
      v03: {
        title: 'Eleven components in one release: overlays, feedback, ⌘K',
        lead: 'The single biggest drop: menu, popover, drawer and a command palette on the CDK, a new feedback category with progress and alert, plus slider, breadcrumbs, pagination, kbd and divider. Releases also started shipping with signed provenance.',
        bullets: [
          'dm-menu: the WAI-ARIA menu pattern with FocusKeyManager, type-ahead, sections and shortcuts',
          'dm-command: a ⌘K palette — grouped, filterable actions, global hotkey, focus trap',
          'dm-popover with an arrow that follows the real flip; DmDrawerService with four placements',
          'New feedback category: dm-progress and dm-alert',
          'dm-slider, dm-breadcrumbs, dm-pagination, dm-kbd and dm-divider',
          'npm publishes now carry signed provenance (Sigstore via GitHub Actions OIDC)',
        ],
      },
      v02: {
        title: 'Packaging done right: ng add, exports and SSR safety',
        lead: 'No new components — a release entirely about being a good npm citizen: a working ng add, strict exports subpaths for the styles, precompiled CSS for Sass-less consumers, and core services verified server-safe.',
        bullets: [
          'ng add @dmaster/ui: installs the CDK, registers overlay styles and wires the global stylesheet',
          'Styles resolve under strict exports-aware tooling: @dmaster/ui/styles/* (Vite, Nx, pnpm)',
          'Precompiled styles/dmaster-ui.css for projects without Sass',
          'rxjs declared as an explicit peer; LICENSE and CHANGELOG ship inside the package',
          'ThemeService, DensityService and ReducedMotionService verified SSR-safe',
        ],
      },
      v01: {
        title: 'The initial public surface: 18 components and a theme system',
        lead: 'Where it started: eighteen standalone, signals-based components across seven categories, a token-driven theme with light/dark/auto and three densities, injectable per-component defaults — and the flat, pill-radius design language that still defines the library.',
        bullets: [
          'Primitives, layout, buttons, forms, navigation, data display and overlays — from day one',
          'Every form control a ControlValueAccessor: template-driven and reactive forms alike',
          'dm-table with a search → sort → paginate pipeline; dm-tabs with a sliding indicator',
          '--dm-* tokens, light/dark/auto via data-dm-theme, three densities via data-dm-density',
          'provideDmasterUI() plus per-component provideXxxDefaults()',
          'Flat fills, pill radii, elastic press — the design language, set from the first commit',
        ],
      },
    },
  },
  pages: {
    overview: {
      lead: 'Every component in the library, live. Built with signals, themed with CSS variables, accessible by default — open any tile to see its docs, playground and API.',
      apiCaption: '',
      a11yItems: [],
      api: {},
      labels: {
        count: 'components',
        open: 'Open docs',
        eyebrow: 'Component library',
        all: 'All',
        categories: 'categories',
        tests: 'tests passing',
        deps: 'runtime deps',
        filterAria: 'Filter by category',
        getStarted: 'Get started',
        copyInstall: 'Copy install command',
        copiedInstall: 'Copied',
        copied: 'Copied',
      },
    },
    spinner: {
      lead: 'Indeterminate loading indicator. Inherits <code>currentColor</code>, so it adapts to any surface — buttons, inputs, empty states — without extra inputs.',
      apiCaption: 'dm-spinner inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-spinner</code> with the <code>SPINNER_DEFAULTS</code> token or the <code>provideSpinnerDefaults</code> helper:',
      a11yItems: [
        'Without <code>label</code> it is decorative (<code>aria-hidden="true"</code>): the surrounding context announces loading.',
        'With <code>label</code> it exposes <code>role="status"</code> + <code>aria-label</code>.',
        'With <code>prefers-reduced-motion: reduce</code> the rotation stops (static arc).',
      ],
      api: {
        size: 'Named size (sm 1rem, md 1.5rem, lg 2rem), pixels (number) or any CSS length.',
        strokeWidth: 'Stroke width in viewBox units (24).',
        label: 'Accessible label. Empty → decorative.',
      },
      labels: {
        inButton: 'Inside other components',
        colors: 'Colors (currentColor)',
        stroke: 'Stroke width',
      },
    },
    badge: {
      lead: 'Chip / status label with a color × variant API. No logic, pure theming: six colors, six variants, two sizes and a radius scale.',
      apiCaption: 'dm-badge inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-badge</code> with the <code>BADGE_DEFAULTS</code> token or the <code>provideBadgeDefaults</code> helper:',
      a11yItems: [
        'Plain text content: read naturally by screen readers.',
        'The <code>dot</code> variant adds a redundant marker so color is not the only state carrier.',
      ],
      api: {
        color: 'Semantic color.',
        variant: 'Visual variant: solid, flat, bordered, light, dot, shadow.',
        size: 'Size scale.',
        radius: 'Corner rounding: sm, md, lg, full (pill).',
      },
      labels: {
        colorsHeading: 'Colors',
        dotHeading: 'Dot variant',
        sizesRadiusHeading: 'Sizes & radius',
        withIconHeading: 'With icon',
      },
    },
    card: {
      lead: 'Surface primitive. Declares <code>container-type: inline-size</code>, so its content can use container queries against the card instead of the viewport.',
      apiCaption: 'dm-card inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-card</code> with the <code>CARD_DEFAULTS</code> token or the <code>provideCardDefaults</code> helper:',
      a11yItems: [
        'The card is a neutral container; if the whole card is clickable, wrap it in a link or button so it gets real semantics and focus.',
        'The hover lift honors reduced-motion via the duration tokens.',
      ],
      api: {
        appearance: 'Surface treatment: elevated, outlined or flat.',
        padding: 'Inner padding scale.',
        interactive: 'Hover lift for clickable cards.',
      },
      labels: {
        interactiveHeading: 'Interactive',
        cardTitle: 'Card title',
        cardBody: 'Cards are the base surface for grouping related content.',
        paddingHeading: 'Padding',
        sectionsHeading: 'Header, body & footer',
        mediaHeading: 'Media & cover',
      },
    },
    avatar: {
      lead: 'Avatar with an automatic fallback chain: image → initials → generic icon. A failed image load falls back silently; changing <code>src</code> retries.',
      apiCaption: 'dm-avatar inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-avatar</code> with the <code>AVATAR_DEFAULTS</code> token or the <code>provideAvatarDefaults</code> helper:',
      a11yItems: [
        'With image: standard <code>&lt;img alt&gt;</code>.',
        'Fallbacks expose <code>role="img"</code> + <code>aria-label</code> (alt → initials).',
      ],
      api: {
        src: 'Image URL. Falls back to initials (then icon) on error.',
        alt: 'Alt for the image / label for the fallback.',
        initials: 'Shown when there is no (working) image.',
        size: 'Named size (2 / 2.5 / 3rem), pixels (number) or CSS length.',
        shape: 'Circle or square.',
        color: 'Semantic color of the initials tint.',
        fallbackSlot:
          'Projected custom fallback: replaces the generic person icon when there is no working image and no initials (import DmAvatarFallbackDirective).',
      },
      labels: {
        fallbackHeading: 'Fallback chain',
        shapes: 'Shapes',
        group: 'Stacked group',
        status: 'With status dot',
      },
    },
    switch: {
      lead: 'Toggle switch (<code>role="switch"</code>). Works standalone with <code>[(checked)]</code> and with Angular forms via <code>ControlValueAccessor</code>. The playground of this very docs app uses it.',
      apiCaption: 'dm-switch inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-switch</code> with the <code>SWITCH_DEFAULTS</code> token or the <code>provideSwitchDefaults</code> helper:',
      a11yItems: [
        '<code>role="switch"</code> + <code>aria-checked</code>; the projected content becomes the label via <code>aria-labelledby</code>.',
        'Without a projected label, pass <code>ariaLabel</code>.',
        '≥44px touch target; thumb motion honors reduced-motion.',
      ],
      api: {
        checked: 'Two-way state: [(checked)] / (checkedChange).',
        color: 'Semantic color of the checked track.',
        disabled: 'Combined with the forms disabled state.',
        size: 'Size scale.',
        inputId: 'Id of the internal button, for external label[for].',
        ariaLabel: 'Accessible name when no label content is projected.',
      },
      labels: {
        notifications: 'Email notifications',
        formValue: 'Form value',
        colors: 'Colors',
        validation: 'Validation with dm-error',
      },
    },
    checkbox: {
      lead: 'Checkbox built on a real native <code>&lt;input type="checkbox"&gt;</code> (form semantics intact) with a custom box. Supports indeterminate and Angular forms via <code>ControlValueAccessor</code>.',
      apiCaption: 'dm-checkbox inputs',
      a11yItems: [
        'Real native input: keyboard, focus and form semantics for free; the projected content is a real label.',
        'Indeterminate uses the native <code>indeterminate</code> property (not <code>aria-checked="mixed"</code>).',
        '≥44px touch target; the check animation honors reduced-motion.',
      ],
      api: {
        checked: 'Two-way state: [(checked)] / (checkedChange).',
        color: 'Semantic color of the checked / indeterminate fill.',
        indeterminate: 'Visual mixed state while not checked.',
        disabled: 'Combined with the forms disabled state.',
        inputId: 'Id of the native input, for external label[for].',
        ariaLabel: 'Accessible name when no label content is projected.',
      },
      labels: {
        terms: 'Accept the terms and conditions',
        indeterminateHeading: 'Indeterminate',
        selectAll: 'Select all',
      },
    },
    formField: {
      lead: 'Composite form field: label + projected native control (<code>dmInput</code>) + hint/error. It wires <code>id</code>, <code>for</code>, <code>aria-describedby</code> and <code>aria-invalid</code> automatically.',
      apiCaption: 'dm-form-field inputs',
      a11yItems: [
        'The label is linked to the control with <code>for</code>; hint and error travel via <code>aria-describedby</code>.',
        'The error uses <code>role="alert"</code> and switches the input to <code>aria-invalid="true"</code>.',
        'Error display is deliberately explicit: the consumer decides when to show which message.',
      ],
      api: {
        label: 'Visible label, wired with for.',
        hint: 'Help text (hidden while error is set).',
        error: 'Error text; non-empty activates the error state.',
        required: 'Shows the * marker (set required on the input too).',
        dmInput:
          'Attribute directive that applies the field styling to native input/textarea/select.',
      },
      labels: {
        emailLabel: 'Email',
        emailHint: 'We never share your email.',
        emailError: 'Enter a valid email address.',
        messageLabel: 'Message',
        withTextarea: 'With textarea',
        inputTypes: 'Input types',
        errorInput: 'Validation with dm-error',
        nameLabel: 'Full name',
        nameRequired: 'Please enter your name.',
        emailRequired: 'Email is required.',
        passwordLabel: 'Password',
        passwordHint: 'At least 8 characters.',
        passwordRequired: 'Password is required.',
        passwordMinLength: 'Password must be at least 8 characters.',
        messageHint: 'Tell us how we can help.',
        quantityLabel: 'Quantity',
        countryLabel: 'Country',
        showPassword: 'Show password',
        hidePassword: 'Hide password',
        disabledLabel: 'Disabled field',
        readonlyLabel: 'Read-only field',
        readonlyHint: 'This value cannot be edited.',
        signupTitle: 'Create your account',
        signupSubtitle: 'Start your free 14-day trial. No credit card required.',
        createAccount: 'Create account',
      },
    },
    searchField: {
      lead: 'A text field specialised for search: a leading magnifier, a trailing clear button that appears once there is text, and search semantics (Escape clears, Enter submits). Color × variant API, and Reactive Forms via <code>ControlValueAccessor</code>.',
      apiCaption: 'dm-search-field inputs & outputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-search-field</code> with the <code>SEARCH_FIELD_DEFAULTS</code> token or the <code>provideSearchFieldDefaults</code> helper:',
      a11yItems: [
        'The clear button carries an <code>aria-label</code> and is taken out of the tab order (<code>tabindex="-1"</code>) — <strong>Escape</strong> already clears from the input.',
        'With a visible <code>label</code> the input is wired via <code>aria-labelledby</code>; without one, pass <code>ariaLabel</code>.',
        '<code>error</code> sets <code>aria-invalid</code> and links the message with <code>aria-describedby</code> (the message is <code>role="alert"</code>).',
        'The native <code>type="search"</code> decorations are hidden so the custom clear button is the single, predictable affordance.',
      ],
      api: {
        value: 'Two-way text value: [(value)] / (valueChange).',
        label: 'Visible label above the field.',
        placeholder: 'Shown while the field is empty.',
        description: 'Help text below the field (hidden while error is set).',
        error: 'Non-empty activates the invalid state.',
        disabled: 'Disables the field; combined with the forms disabled state.',
        readonly: 'Read-only: not editable and no clear button.',
        required: 'Shows the * marker and sets aria-required.',
        clearable: 'Shows the × button (and Escape-to-clear) once there is text.',
        color: 'Semantic color for the focus ring and caret.',
        variant: 'Field surface: flat, bordered, faded, underlined.',
        size: 'Field height (32 / 40 / 48px).',
        radius: 'Corner rounding (md default, matching the field family; full = pill).',
        name: 'name for the native input (form submission / autofill).',
        ariaLabel: 'Accessible name when no visible label.',
        clearAriaLabel: 'ARIA label for the clear button (localize for multilingual apps).',
        search: 'Emitted with the current value when the user presses Enter.',
        cleared: 'Emitted after the field is cleared (× button or Escape).',
      },
      labels: {
        searchLabel: 'Search',
        searchPlaceholder: 'Search…',
        searchHint: 'Press Enter to submit, Escape to clear.',
        eventsHeading: 'Submit & clear events',
        eventsPlaceholder: 'Type and press Enter…',
        lastEvent: 'Last event',
        formsHeading: 'Reactive Forms',
        formValue: 'Form value',
        contactsTitle: 'Contacts',
        contactsSearchLabel: 'Search contacts',
        contactsSearchPlaceholder: 'Search by name or email…',
        noMatches: 'No matches for',
      },
    },
    errorMessage: {
      lead: 'A validation error line for form fields — the standalone, Angular-native equivalent of <code>mat-error</code>. Danger color, a subtle entrance, and <code>role="alert"</code> so it is announced the moment it appears. <code>dm-error</code> carries no icon of its own — its content is fully projected, so add one (e.g. <code>&lt;dm-icon&gt;</code>) when you want one. Drop it inside <code>dm-form-field</code> and it is positioned and wired to <code>aria-describedby</code> automatically.',
      apiCaption: 'dm-error inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-error</code> with the <code>ERROR_DEFAULTS</code> token or the <code>provideErrorDefaults</code> helper:',
      a11yItems: [
        'The host is <code>role="alert"</code>, so assistive tech announces the message when it is inserted — render it conditionally (<code>@if</code>) when validation fails.',
        'Inside <code>dm-form-field</code> the control’s <code>aria-invalid</code> and <code>aria-describedby</code> are wired to the message automatically.',
        'The content is fully projected, so it accepts plain text, rich markup, or your own icon — mark a decorative icon <code>aria-hidden</code> yourself.',
      ],
      api: {
        size: 'Text scale: sm (xs) or md (sm).',
        content:
          'The projected message — text, markup, or an icon (e.g. <code>&lt;dm-icon&gt;</code>).',
      },
      labels: {
        sampleMessage: 'This field is required',
        smallMessage: 'Small message',
        mediumMessage: 'Medium message',
        withIconHeading: 'With an icon',
        inFieldHeading: 'Inside a form field (Reactive Forms)',
        emailLabel: 'Email',
        emailPlaceholder: 'you@example.com',
        emailRequired: 'Email is required',
        emailInvalid: 'Enter a valid email address',
        submit: 'Submit',
        perErrorHeading: 'One message per error',
        usernameLabel: 'Username',
        usernamePlaceholder: 'e.g. ada_lovelace',
        usernameHint: '3–16 characters, letters, numbers and underscores.',
        usernameRequired: 'Username is required',
        usernameMinlength: 'Username must be at least 3 characters',
        usernamePattern: 'Only letters, numbers and underscores',
        checkoutTitle: 'Payment details',
        cardholderLabel: 'Cardholder name',
        cardholderPlaceholder: 'Name as printed on the card',
        cardholderRequired: 'Cardholder name is required',
        cardNumberLabel: 'Card number',
        cardNumberRequired: 'Card number is required',
        cardNumberPattern: 'Enter a valid 16-digit card number',
        cvcLabel: 'CVC',
        cvcRequired: 'CVC is required',
        cvcPattern: 'CVC must be 3 or 4 digits',
        payNow: 'Pay now',
      },
    },
    colorPicker: {
      lead: 'A full color picker: a draggable saturation/brightness area, a hue slider, an optional alpha slider, an editable hex input and quick swatches — in a field-family trigger that opens a CDK overlay. Two-way <code>value</code> is a hex string, and it works with Reactive Forms via <code>ControlValueAccessor</code>.',
      apiCaption: 'dm-color-picker inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-color-picker</code> with the <code>COLOR_PICKER_DEFAULTS</code> token or the <code>provideColorPickerDefaults</code> helper:',
      a11yItems: [
        'The trigger is a button with <code>aria-haspopup="dialog"</code> and <code>aria-expanded</code>; the panel is a <code>role="dialog"</code>.',
        'The saturation/brightness area and the hue/alpha tracks are <code>role="slider"</code> with <code>aria-valuetext</code>, and respond to the arrow keys (Shift for larger steps).',
        'Escape closes the panel and returns focus to the trigger; a polite live region announces the hex value as it changes.',
        'Each swatch is a button labelled with its hex value.',
      ],
      api: {
        value: 'Two-way hex color (#rrggbb, or #rrggbbaa with alpha).',
        showAlpha: 'Show the alpha (opacity) slider and emit 8-digit hex.',
        swatches: 'Quick-pick palette shown in the panel.',
        label: 'Visible label above the trigger.',
        placeholder: 'Shown while no color is selected.',
        description: 'Help text below the trigger (hidden while error is set).',
        error: 'Non-empty activates the invalid state.',
        disabled: 'Disables the trigger; combined with the forms disabled state.',
        required: 'Shows the * marker and sets aria-required.',
        clearable: 'Shows the × button once a color is set.',
        color: 'Semantic color for the focus ring.',
        variant: 'Trigger surface: flat, bordered, faded, underlined.',
        size: 'Trigger height (32 / 40 / 48px).',
        radius: 'Corner rounding (md default; full = pill).',
        ariaLabel: 'Accessible name when no visible label.',
        format:
          'Serialization of the emitted value: hex, rgb() or hsl() (the alpha part follows showAlpha).',
      },
      labels: {
        pickLabel: 'Brand color',
        pickPlaceholder: 'Pick a color',
        alphaHeading: 'With alpha',
        swatchesHeading: 'Custom swatches',
        formsHeading: 'Reactive Forms',
        formValue: 'Form value',
        selected: 'Selected',
        brandTitle: 'Brand color',
        brandDesc: 'Pick your brand color, watch the UI follow.',
        brandLabel: 'Brand',
        tileSolid: 'Solid',
        tileOutlined: 'Outlined',
        tileTint: 'Tint',
      },
    },
    datePicker: {
      lead: 'A calendar-overlay date field with layered day → month → year navigation. No date library: the calendar math is pure and timezone-safe, and every month/weekday name plus the trigger text comes from <code>Intl</code>, so it follows the <code>locale</code> input and works under SSR. Color × variant API and Reactive Forms via <code>ControlValueAccessor</code>.',
      apiCaption: 'dm-date-picker inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-date-picker</code> with the <code>DATE_PICKER_DEFAULTS</code> token or the <code>provideDatePickerDefaults</code> helper:',
      a11yItems: [
        'The trigger is a button with <code>aria-haspopup="dialog"</code> and <code>aria-expanded</code>; the panel is a <code>role="dialog"</code> labelled by the visible month.',
        'The calendar is a <code>role="grid"</code> with <code>columnheader</code> weekdays; each day is a <code>gridcell</code> button with a localized full-date <code>aria-label</code>, <code>aria-selected</code>, and <code>aria-current="date"</code> for today.',
        'Roving-tabindex focus follows the arrow keys (crossing month edges); PageUp/Down change month, Shift+PageUp/Down change year; Enter selects, Escape closes and returns focus to the trigger.',
        'A polite live region announces the month as it changes, and disabled days set <code>aria-disabled</code>.',
      ],
      api: {
        value: 'Two-way selected date (Date | null), normalized to local midnight.',
        range:
          'Switches the calendar to range mode: selection flows through rangeValue instead of value.',
        rangeValue:
          'Two-way selected range ({ start, end }) in range mode. First click sets the start, the second the end.',
        min: 'Earliest selectable date (inclusive); earlier days are disabled.',
        max: 'Latest selectable date (inclusive); later days are disabled.',
        isDateDisabled: 'Predicate to disable arbitrary days (weekends, holidays…).',
        firstDayOfWeek:
          "Leftmost calendar column: 0 (Sunday) … 6 (Saturday), or 'auto' (default) to follow the locale's week convention (Monday in Spain, Sunday in the US…).",
        locale: 'BCP-47 locale for all names/formatting; omit for the runtime default.',
        displayFormat: 'Intl options controlling how the trigger renders the date.',
        weekdayFormat: 'Weekday header length: narrow or short.',
        showTodayButton: 'Shows the “Today” quick-jump in the panel footer.',
        closeOnSelect: 'Close the panel right after a day is picked.',
        dayTemplate:
          'Projected template replacing the day number inside each day cell. Context: the day Date plus selected / disabled / today / outside flags; the button keeps its state attributes and interaction.',
        label: 'Visible label above the trigger.',
        placeholder: 'Shown while no date is selected.',
        description: 'Help text below the trigger (hidden while error is set).',
        error: 'Non-empty activates the invalid state.',
        disabled: 'Disables the trigger; combined with the forms disabled state.',
        required: 'Shows the * marker and sets aria-required.',
        clearable: 'Shows the × button (and Delete/Backspace) once a date is set.',
        color: 'Semantic color for the focus ring and the selected-day fill.',
        variant: 'Trigger surface: flat, bordered, faded, underlined.',
        size: 'Trigger height (32 / 40 / 48px).',
        radius: 'Corner rounding (md default, matching the field family; full = pill).',
        ariaLabel: 'Accessible name when no visible label.',
      },
      labels: {
        pickLabel: 'Date',
        pickPlaceholder: 'Pick a date',
        rangeHeading: 'Date range',
        rangeLabel: 'Trip dates',
        rangePlaceholder: 'Pick a range',
        selectedRange: 'Selected range',
        localizedHeading: 'Localized calendars',
        globalLocaleHeading: 'Global locale (app-wide)',
        globalLocaleNote:
          'Every picker on this page follows the header language selector — no per-instance locale. Switch the language up top and watch this calendar re-format live.',
        constraintsHeading: 'Min, max & disabled days',
        appointmentLabel: 'Appointment',
        constraintsHint: 'This month only, weekdays.',
        selected: 'Selected',
        sizeSm: 'Small',
        sizeMd: 'Medium',
        sizeLg: 'Large',
        validationHeading: 'Validation (with dm-error)',
        chooseDate: 'Please choose a date',
        formsHeading: 'Reactive Forms',
        formValue: 'Form value',
        cabinTitle: 'Cabin by the lake',
        cabinSubtitle: 'Sleeps 4 · Free cancellation',
        checkInOut: 'Check-in — Check-out',
        nights: 'nights',
        reserve: 'Reserve',
      },
    },
    select: {
      lead: 'Dropdown with a color × variant API. Single or multiple selection with chips, an optional inline filter, option groups, and select-all / clear-all. Full keyboard support (arrows, Home/End, Enter, Escape, typeahead), CDK-anchored panel that matches the trigger width, and Reactive Forms via <code>ControlValueAccessor</code>. Provide a <code>loadFn</code> and it becomes <strong>server-driven</strong>: options load in pages (infinite scroll or a button), the filter turns into a debounced server search, and in-flight requests cancel automatically.',
      apiCaption: 'dm-select inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-select</code> with the <code>SELECT_DEFAULTS</code> token or the <code>provideSelectDefaults</code> helper:',
      a11yItems: [
        'Trigger with <code>role="combobox"</code>, <code>aria-haspopup="listbox"</code>, <code>aria-expanded</code>, <code>aria-controls</code>, <code>aria-activedescendant</code>.',
        'Panel with <code>role="listbox"</code>; each option carries <code>aria-selected</code> and <code>aria-disabled</code>.',
        'Full keyboard: Enter/Space open (or select), Escape close, ArrowUp/Down move, Home/End jump, Tab leaves naturally, printable characters trigger typeahead. With a filter, the first Escape clears the text and the second closes.',
        'Disabled items are skipped by keyboard and cannot be clicked.',
        'Async mode: the listbox sets <code>aria-busy</code> while loading and a polite live region announces the loading state.',
      ],
      api: {
        items: 'Flat options or { label, items } groups to render in the panel.',
        multiple: 'Selects more than one value; chips replace the single-value label.',
        value: 'Two-way state for single mode: [(value)] / (valueChange).',
        values: 'Two-way array for multiple mode: [(values)] / (valuesChange).',
        compareWith:
          'Match a value against options by identity (objects by field), not by reference.',
        filterable: 'Shows an inline search box in the panel that filters the options.',
        filterPlaceholder: 'Placeholder for the inline filter input.',
        noResultsLabel: 'Message shown when the filter matches no options.',
        selectAllLabel: 'Label for the select-all action (multiple mode; hidden if empty).',
        clearAllLabel: 'Label for the clear-all action (multiple mode; hidden if empty).',
        label: 'Visible label above the trigger.',
        placeholder: 'Shown while nothing is selected.',
        description: 'Help text below the trigger.',
        error: 'Non-empty activates the invalid state.',
        disabled: 'Combined with the forms disabled state.',
        required: 'Shows the * marker and sets aria-required.',
        color: 'Semantic color for focus ring and selected item highlight.',
        variant: 'Trigger surface: flat, bordered, faded, underlined.',
        size: 'Trigger height (32 / 40 / 48px).',
        radius: 'Corner rounding.',
        ariaLabel: 'Accessible name when no visible label.',
        clearable: 'Shows an × button to clear the selection. Keyboard: Delete / Backspace.',
        clearAriaLabel: 'ARIA label for the clear button (localize for multilingual apps).',
        loadFn:
          'Switches to server-driven mode: pages load through this Observable-returning fetch function and items is ignored.',
        pageSize: 'Async: items requested per page.',
        searchDebounceMs: 'Async: quiet ms before the filter text reloads page 0.',
        loadMoreMode: 'Async: infinite (IntersectionObserver) or an explicit Load-more button.',
        loadMoreLabel: 'Async: label of the Load-more button.',
        loadingLabel: 'Async: label for the loading row and the polite live region.',
        selectedItems:
          'Async: known items to resolve labels when the selected value is not in the loaded pages.',
        filterClearAriaLabel: 'ARIA label for the filter clear (×) button.',
        optionTemplate:
          'Projected template replacing the option body (label + description); the check indicator, states and selection handling stay built-in. Trigger chips keep the plain label.',
      },
      labels: {
        pet: 'Pet',
        pets: 'Pets',
        pickOne: 'Pick a pet',
        pickSome: 'Pick some pets',
        petHint: 'Only these pets are covered by the insurance.',
        optionTemplate: 'Custom option template',
        statusLabel: 'Status',
        statusPlaceholder: 'Pick a status',
        formsHeading: 'Reactive Forms',
        formValue: 'Form value',
        clearableHeading: 'Clearable',
        multipleHeading: 'Multiple with chips',
        filterableHeading: 'Inline filter',
        groupedHeading: 'Option groups',
        multiFilterHeading: 'Multiple + filter + select all',
        searchPlaceholder: 'Search…',
        noMatches: 'No matches',
        selectAll: 'Select all',
        clearAll: 'Clear',
        validationHeading: 'Validation with dm-error',
        petRequired: 'Pick a pet to continue',
        inviteTitle: 'Invite a team member',
        inviteSubtitle: 'Choose a role and the teams they will join.',
        roleLabel: 'Role',
        rolePlaceholder: 'Pick a role',
        roleRequired: 'A role is required',
        teamsLabel: 'Teams',
        teamsPlaceholder: 'Pick teams',
        teamsHint: 'They get access on day one.',
        teamsRequired: 'Pick at least one team',
        sendInvite: 'Send invite',
        inviteSending: 'Sending…',
        inviteSent: 'Invite sent',
        asyncHeading: 'Server-driven (async)',
        asyncButtonHeading: 'Async · Load more button',
        loadFnHeading: 'The loadFn contract',
        assigneeLabel: 'Assignee',
        assigneePlaceholder: 'Pick a user',
        searchUsers: 'Search users…',
        loadingUsers: 'Loading…',
        noUsersFound: 'No users found',
        loadMore: 'Load more',
      },
    },
    autocomplete: {
      lead: 'A text field that suggests options as you type. Unlike <code>dm-select</code>, the trigger is a free-text input: users type anything, filter the suggestions, and either pick one or keep their own text. Full keyboard navigation, a CDK-anchored panel, and Reactive Forms via <code>ControlValueAccessor</code>.',
      apiCaption: 'dm-autocomplete inputs & outputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-autocomplete</code> with the <code>AUTOCOMPLETE_DEFAULTS</code> token or the <code>provideAutocompleteDefaults</code> helper:',
      a11yItems: [
        'Input with <code>role="combobox"</code>, <code>aria-autocomplete="list"</code>, <code>aria-expanded</code>, <code>aria-controls</code> and <code>aria-activedescendant</code>.',
        'Suggestion panel with <code>role="listbox"</code>; each option carries <code>aria-selected</code> and <code>aria-disabled</code>.',
        'Full keyboard: ArrowUp/Down move (wrapping), Home/End jump, Enter commits the active option, Escape closes, Tab leaves naturally.',
        'Disabled options are skipped by the keyboard and cannot be clicked; free text is always preserved.',
      ],
      api: {
        options: 'Array of { value, label, description?, disabled? } suggestions.',
        value: 'Two-way text value (the free text in the field): [(value)] / (valueChange).',
        optionSelected: 'Emits the chosen DmAutocompleteOption when the user picks a suggestion.',
        openChange: 'Emits when the suggestions overlay opens (true) or closes (false).',
        optionTemplate:
          'Projected template replacing the option row content (context: option, index, active).',
        label: 'Visible label above the field.',
        placeholder: 'Shown while the field is empty.',
        description: 'Help text below the field.',
        error: 'Non-empty activates the invalid state.',
        disabled: 'Combined with the forms disabled state.',
        required: 'Shows the * marker and sets aria-required.',
        clearable: 'Shows an × button to clear the text.',
        openOnFocus: 'Opens the suggestions on focus, before any text is typed.',
        filter: 'Client-side filtering. Set false for server-driven options shown verbatim.',
        filterFn: 'Custom matcher for the built-in filter (e.g. match on a description).',
        noResultsLabel: 'Message shown when no option matches (hidden if empty).',
        color: 'Semantic color for the focus ring and caret.',
        variant: 'Field surface: flat, bordered, faded, underlined.',
        size: 'Field height (32 / 40 / 48px).',
        radius: 'Corner rounding.',
        ariaLabel: 'Accessible name when there is no visible label.',
        clearAriaLabel: 'ARIA label for the clear button (localize for multilingual apps).',
      },
      labels: {
        fruit: 'Fruit',
        typeToSearch: 'Type to search…',
        basicHeading: 'Basic',
        openOnFocusHeading: 'Open on focus',
        eventsHeading: 'Selection event',
        formsHeading: 'Reactive Forms',
        noMatches: 'No matches',
        lastSelected: 'Last selected',
        formValue: 'Field value',
        validationHeading: 'Validation with dm-error',
        cityLabel: 'City',
        cityPlaceholder: 'Start typing a city…',
        cityRequired: 'Enter a city to continue',
        shipTitle: 'Ship to',
        shipSubtitle: 'Where should we send your order?',
        streetLabel: 'Street address',
        streetPlaceholder: '221B Baker Street',
        streetRequired: 'Street address is required',
        continueLabel: 'Continue',
        shipChecking: 'Checking address…',
        shipDone: 'Address saved',
      },
    },
    tooltip: {
      lead: 'Text tooltip on any element, built on the CDK Overlay. Shows on hover (with delay) and keyboard focus (immediate); Escape closes it; flips when there is no room.',
      apiCaption: 'dmTooltip inputs and defaults',
      defaultsDesc:
        'Position and delays are configurable per app with <code>TOOLTIP_DEFAULTS</code> or <code>provideTooltipDefaults</code>:',
      a11yItems: [
        'Panel with <code>role="tooltip"</code>, referenced from the trigger via <code>aria-describedby</code> while visible.',
        'Keyboard support: shows on focus, hides on blur and Escape.',
        'The entrance animation honors reduced-motion via the duration tokens.',
      ],
      api: {
        dmTooltip: 'Tooltip text (required). Empty → never shown.',
        dmTooltipPosition: 'Preferred placement; auto-flips: top, bottom, left, right.',
        showDelay: 'Default hover delay before showing (300ms). Global default.',
        hideDelay: 'Default delay before hiding (100ms). Global default.',
        disabled:
          'Disables the tooltip: it never opens, an open panel closes, and aria-describedby is removed.',
      },
      labels: {
        trigger: 'Hover or focus me',
        iconButtons: 'Icon-only buttons',
        conditional: 'Conditional & disabled',
        tipOn: 'Favorited — the share tooltip is now active',
        tipOff: 'Not favorited — share is disabled and shows no tooltip',
        toggle: 'Toggle favorite',
        onContent: 'On text, badges & avatars',
        docTitle: 'Release notes — v0.5',
        docBody:
          'Every control in the toolbar above is icon-only. Hover or tab through them: the tooltip names the action and its shortcut, while the icon label does the same for assistive tech.',
        saved: 'Saved',
        publish: 'Publish',
      },
    },
    dialog: {
      lead: 'Modal dialog service, a thin wrapper over the CDK Dialog: focus trap, Escape/backdrop and <code>aria-modal</code> come from the CDK; the panel ships the library look. Requires <code>overlay-prebuilt.css</code> once per app.',
      apiCaption: 'DmDialogConfig options',
      defaultsDesc:
        'Panel size and <code>disableClose</code> can be changed app- or route-wide with <code>DIALOG_DEFAULTS</code> or <code>provideDialogDefaults</code>:',
      a11yItems: [
        'Focus trap and focus restore on close (CDK).',
        '<code>role="dialog"</code> + <code>aria-modal="true"</code>; name it with <code>ariaLabel</code>.',
        'Escape and backdrop click close it unless <code>disableClose</code> is set.',
      ],
      api: {
        data: 'Injected into the content component via DIALOG_DATA.',
        size: 'Panel width: sm 22rem, md 30rem, lg 42rem.',
        disableClose: 'Blocks backdrop click and Escape.',
        ariaLabel: 'Accessible name of the dialog.',
        openTemplate:
          'open() also accepts a TemplateRef, so a one-off dialog needs no dedicated content component.',
        confirm:
          'Opens a ready-made confirmation dialog and resolves true on confirm, false on cancel, Escape or backdrop click. title, confirmLabel and cancelLabel are required — the library ships no copy.',
      },
      labels: {
        open: 'Open dialog',
        demoTitle: 'Delete component?',
        demoBody: 'This action cannot be undone. The component will be removed from the library.',
        cancel: 'Cancel',
        confirm: 'Delete',
        result: 'Last result',
        close: 'Close',
        save: 'Save',
        basicTitle: 'Welcome to @dmaster/ui',
        basicBody:
          'This dialog is a plain content component: it reads its copy from DIALOG_DATA and closes itself through DialogRef.',
        confirmDialog: 'Confirm dialog',
        formDialog: 'Form dialog',
        formTitle: 'New project',
        formNameLabel: 'Project name',
        formNamePlaceholder: 'e.g. Design system v2',
        formNameRequired: 'A name is required',
        sm: 'Small',
        md: 'Medium',
        lg: 'Large',
        persistent: 'Persistent (disableClose)',
        openPersistent: 'Open persistent dialog',
        persistentTitle: 'Unsaved changes',
        persistentBody:
          'Escape and the backdrop are ignored here — the dialog only closes through the button below.',
        projectName: 'Design system v2',
        projectMeta: 'Updated 2 days ago · 3 members',
        projectActive: 'Active',
        projectDeleted: 'Deleted',
        projectDeleteTitle: 'Delete project?',
        projectDeleteBody:
          'The project and its history will be removed. You can restore it from this demo.',
        restore: 'Restore',
        confirmDemo: 'Confirm helper',
        confirmHelperOpen: 'Delete file',
        confirmHelperTitle: 'Delete this file?',
        confirmHelperMessage: 'This action cannot be undone.',
      },
    },
    toast: {
      lead: 'Notification queue. Toasts stack bottom-right, auto-dismiss (configurable) and are announced politely. Requires <code>overlay-prebuilt.css</code> once per app.',
      apiCaption: 'DmToastOptions',
      defaultsDesc:
        'Duration, dismiss button and its aria-label (the only built-in copy in the library) are configurable with <code>TOAST_DEFAULTS</code> or <code>provideToastDefaults</code>:',
      a11yItems: [
        'Each toast is <code>role="status"</code>: announced without interrupting the user.',
        'The dismiss button has a configurable <code>aria-label</code> and a ≥44px touch target.',
        'The entrance animation honors reduced-motion via the duration tokens.',
      ],
      api: {
        variant: 'Semantic color + icon: neutral, success, warning, danger.',
        duration: 'Auto-dismiss in ms; 0 disables it.',
        dismissible: 'Shows the dismiss button.',
        title: 'Optional bold title rendered above the message.',
        action:
          'Action button: runs its handler and then dismisses the toast; afterDismissed resolves as usual.',
        position:
          'Global default (DmToastDefaults): viewport placement of the toast stack, read once when the first toast creates the container.',
      },
      labels: {
        show: 'Show toast',
        helpers: 'Variant helpers',
        message: 'Changes saved successfully',
        sticky: 'Sticky toast (duration 0)',
        neutralMessage: 'Draft autosaved',
        warningMessage: 'Storage almost full',
        dangerMessage: 'Something went wrong',
        duration: 'Duration',
        short: 'Short (1.5 s)',
        defaultDuration: 'Default (4 s)',
        shortMessage: 'Copied to clipboard',
        dismissible: 'Dismissible',
        withDismiss: 'With dismiss button',
        noDismiss: 'Auto-dismiss only',
        withDismissMessage: 'You can close me',
        noDismissMessage: 'I go away on my own',
        programmatic: 'Programmatic control',
        startUpload: 'Start upload',
        finishUpload: 'Finish upload',
        dismissAll: 'Dismiss all',
        uploadingMessage: 'Uploading photos…',
        uploadedMessage: '12 photos uploaded',
        queue: 'Queue and stacking',
        fireMany: 'Fire 3 toasts',
        queueFirst: 'Syncing library…',
        queueSecond: '3 files imported',
        queueThird: '2 duplicates skipped',
        actionDemo: 'Action button',
        archive: 'Archive conversation',
        archivedTitle: 'Archived',
        archivedMessage: 'Conversation moved to the archive',
        undo: 'Undo',
        restoredMessage: 'Conversation restored',
      },
    },
    accordion: {
      lead: 'Vertical collapsible sections. Choose single or multiple selection, four visual variants and smooth CSS-only expand/collapse animations that honor <code>prefers-reduced-motion</code>.',
      apiCaption: 'dm-accordion / dm-accordion-item inputs',
      defaultsDesc:
        'Change the defaults with the <code>ACCORDION_DEFAULTS</code> token or the <code>provideAccordionDefaults</code> helper.',
      a11yItems: [
        'Header is a native <code>&lt;button&gt;</code> with <code>aria-expanded</code> and <code>aria-controls</code>; panel is a <code>role="region"</code> labelled by the header.',
        'When collapsed, panels use both <code>aria-hidden="true"</code> and <code>inert</code> — screen readers skip them and focus never lands inside a hidden panel.',
        'Keyboard: <kbd>Enter</kbd>/<kbd>Space</kbd> toggle; <kbd>Arrow↓</kbd>/<kbd>Arrow↑</kbd> cycle focus across enabled triggers; <kbd>Home</kbd>/<kbd>End</kbd> jump to first/last.',
      ],
      api: {
        selectionMode: 'single: one open at a time; multiple: any combination.',
        expandedValues: 'Two-way string[] of open item values.',
        variant: 'Visual style: light, bordered, shadow, splitted.',
        disabled: 'Disables every item at once.',
        value: 'Item identifier (required per item).',
        title: 'Header text of the item.',
        subtitle: 'Optional secondary line under the title.',
        icon: 'Leading icon (content projection). Use width="1em" height="1em" on SVG so it scales with font-size.',
      },
      labels: {
        single: 'Single selection',
        multiple: 'Multiple selection',
        variants: 'Variants',
        icon: 'With icon slot',
        disabled: 'Disabled item',
      },
    },
    radioGroup: {
      lead: 'Radio group with roving tabindex and full keyboard navigation. Works standalone via <code>[(value)]</code> or with Angular forms via <code>ControlValueAccessor</code>.',
      apiCaption: 'dm-radio-group / dm-radio inputs',
      defaultsDesc:
        'Change the defaults with the <code>RADIO_DEFAULTS</code> token or the <code>provideRadioDefaults</code> helper.',
      a11yItems: [
        'Host is <code>role="radiogroup"</code>; each item is <code>role="radio"</code> with <code>aria-checked</code>.',
        'Roving tabindex: <kbd>Tab</kbd> enters the group once; <kbd>Arrow</kbd> keys move and select; <kbd>Home</kbd>/<kbd>End</kbd> jump.',
        'A hidden <code>&lt;input name&gt;</code> serializes the value in native <code>&lt;form&gt;</code> submissions.',
      ],
      api: {
        name: 'Attribute name of the hidden input for native form submission. Optional (auto-generated id).',
        value: 'Currently selected value (two-way).',
        color: 'Semantic color of the checked state.',
        size: 'Control size (sm, md, lg).',
        orientation: 'Layout: vertical or horizontal.',
        disabled: 'Disables the whole group.',
        radioValue: 'Value of the individual radio (required per item).',
        ariaLabel: 'Accessible name of the group when there is no visible caption.',
        radioDisabled: "Disables just this option (combined with the group's disabled).",
      },
      labels: {
        colors: 'Colors',
        horizontal: 'Horizontal',
        forms: 'With Angular forms',
        selected: 'Selected:',
        orientation: 'Orientation',
        vertical: 'Vertical',
        disabledOptions: 'Disabled options',
        singleOption: 'Single option',
        wholeGroup: 'Whole group',
        unavailable: 'unavailable',
        shippingLegend: 'Shipping method',
        shippingStandard: 'Standard (3–5 days)',
        shippingExpress: 'Express (1–2 days)',
        shippingPickup: 'Store pickup',
        shippingRequired: 'Choose a shipping method to continue.',
        continue: 'Continue',
        reset: 'Reset',
        planTitle: 'Choose your plan',
        planSubtitle: 'Switch or cancel at any time.',
        planFreeDesc: 'For side projects. 1 seat.',
        planProDesc: 'Unlimited projects. 5 seats.',
        planTeamDesc: 'SSO, audit log. Unlimited seats.',
        recommended: 'Recommended',
        perMonth: '/mo',
      },
    },
    tabs: {
      lead: 'Tabs with full keyboard navigation, five visual variants and horizontal or vertical placement. Colors mapped from the semantic palette. The <code>segment</code> variant renders as an iOS-style segmented control.',
      apiCaption: 'dm-tabs / dm-tab / dm-tab-panel inputs',
      defaultsDesc:
        'Change the defaults with the <code>TABS_DEFAULTS</code> token or the <code>provideTabsDefaults</code> helper.',
      a11yItems: [
        'Tablist is <code>role="tablist"</code>; tabs are <code>role="tab"</code>; panels are <code>role="tabpanel"</code> with <code>aria-labelledby</code>.',
        'Roving tabindex + arrow-key navigation (horizontal or vertical depending on <code>placement</code>). <kbd>Home</kbd>/<kbd>End</kbd> jump to first/last enabled.',
        'Disabled tabs are skipped by keyboard navigation and cannot be activated by click.',
      ],
      api: {
        selectedValue: 'Value of the currently active tab (two-way).',
        color: 'Semantic color of the active tab.',
        variant: 'Visual style: solid, bordered, light, underlined, segment.',
        size: 'Tab height and padding (sm, md, lg).',
        radius: 'Corner rounding; ignored by underlined.',
        placement: 'top (horizontal) or start (vertical).',
        fullWidth: 'Stretch tabs to share the full width equally (default on).',
        divider: 'Separator rule under the tablist (light / underlined).',
        tabValue: 'Value of the individual tab (required per tab).',
        panelValue: 'Value of the panel — must match its tab.',
        disabled: 'Disables a tab.',
        lazy: "Defer each panel's content until its tab is first activated.",
      },
      labels: {
        variants: 'Variants',
        segment: 'Segment (segmented control)',
        segmentIcons: 'Segment with icons',
        vertical: 'Vertical placement',
        colors: 'Colors',
        fullWidth: 'Full width',
        disabled: 'Disabled tab',
      },
    },
    table: {
      lead: 'A premium, data-driven table. Out of the box it searches, sorts, selects and paginates — all internally, all controllable through two-way models. Composes <code>dm-checkbox</code> for selection and <code>dm-skeleton</code> for loading, on semantic HTML.',
      apiCaption: 'dm-table inputs and outputs',
      defaultsDesc:
        'Change the defaults with the <code>TABLE_DEFAULTS</code> token or the <code>provideTableDefaults</code> helper.',
      a11yItems: [
        'Semantic <code>&lt;table&gt;</code>, <code>&lt;thead&gt;</code>, <code>&lt;tbody&gt;</code>, <code>&lt;th scope="col"&gt;</code>, optional <code>&lt;caption&gt;</code> — read natively by screen readers.',
        'Sortable columns expose <code>aria-sort</code>; selected rows expose <code>aria-selected</code>; the table sets <code>aria-busy</code> while loading.',
        'The search box is a real <code>&lt;input type="search"&gt;</code>; selection checkboxes and pager buttons carry descriptive ARIA labels — all overridable for i18n.',
      ],
      api: {
        columns: 'Array of column definitions.',
        data: 'The FULL dataset — the table derives the visible rows.',
        rowKey: 'Row identity (row, i) => key. Required for reliable selection.',
        density: 'compact, comfortable (default) or spacious.',
        variant: 'default, striped or bordered.',
        hover: 'Highlight rows on hover.',
        sticky: 'Header sticks to the top of a scrolling container.',
        caption: 'Table caption (screen-reader friendly).',
        searchable: 'Show the search box and filter rows by the term.',
        searchTerm: 'Two-way bound search term.',
        selectionMode: 'none, single or multiple row selection.',
        selectedKeys: 'Two-way bound array of selected row keys.',
        pageSize: 'Rows per page; 0 disables pagination.',
        page: '1-indexed current page (two-way).',
        pageSizeOptions: 'Options for the rows-per-page selector.',
        loading: 'Show a skeleton loading state.',
        virtualScroll:
          'Render rows inside a CDK virtual-scroll viewport so thousands of rows stay light in the DOM. Requires a fixed rowHeight.',
        rowHeight: 'Fixed row height in px used to virtualize; must match the real row height.',
        viewportHeight: 'CSS height of the scroll viewport when virtualScroll is on.',
        sortState: 'Current sort ({column, direction}); two-way.',
        manualProcessing: 'Disable internal filter/sort/paginate for server-side data.',
        rowClick: 'Emitted with {row, index} on row click.',
        sortChange: 'Emitted when the sort changes.',
        selectionChange: 'Emitted with the selected rows.',
        pageChange: 'Emitted when the page or page size changes.',
        searchChange: 'Emitted when the search term changes.',
        cellTemplate:
          'Projected template rendering rich cells for one column key (context: row, index, column).',
        emptyTemplate: 'Projected template replacing the built-in empty block (context: filtered).',
      },
      labels: {
        fullFeatured: 'Search, select & paginate',
        customCells: 'Custom cells (templates)',
        selection: 'Row selection',
        pagination: 'Pagination',
        virtualScroll: 'Virtual scroll (1,000 rows)',
        densities: 'Densities',
        variants: 'Variants',
        states: 'Loading & empty states',
        searchPlaceholder: 'Search members…',
        rowsPerPage: 'Rows per page',
        empty: 'No members yet',
        noResults: 'No members match your search',
        toggleLoading: 'Toggle loading',
        nothingSelected: 'No rows selected',
        selectedPrefix: 'Selected',
        membersUnit: 'members',
        compositionSubtitle: 'Manage who has access to this workspace.',
        statusActive: 'active',
        statusAway: 'away',
        statusInvited: 'invited',
        invite: 'Invite',
        selectedSuffix: 'selected',
        clearSelection: 'Clear',
        remove: 'Remove',
      },
    },
    tree: {
      lead: 'Hierarchical, data-driven tree view implementing the full WAI-ARIA Tree View pattern: roving tabindex, complete keyboard model and correct ARIA metadata on every node. Pass the whole hierarchy as <code>nodes</code>; selection and expansion are two-way models you can drive or observe. No Angular Material dependency.',
      apiCaption: 'dm-tree inputs, models, outputs and methods',
      defaultsDesc:
        'Change the defaults of every <code>dm-tree</code> with the <code>TREE_DEFAULTS</code> token or the <code>provideTreeDefaults</code> helper.',
      a11yItems: [
        'Container is <code>role="tree"</code> with <code>aria-label</code>/<code>aria-labelledby</code>, plus <code>aria-multiselectable="true"</code> in multiple mode. Child lists wrap in <code>role="group"</code>.',
        'Every node is <code>role="treeitem"</code> with <code>aria-level</code>, <code>aria-setsize</code>, <code>aria-posinset</code>, <code>aria-selected</code> (omitted when selection is off) and <code>aria-expanded</code> on parents only.',
        '<strong>Roving tabindex</strong>: exactly one node is tabbable; <kbd>↑</kbd>/<kbd>↓</kbd> move focus, <kbd>→</kbd>/<kbd>←</kbd> expand/collapse then descend/ascend, <kbd>Home</kbd>/<kbd>End</kbd> jump, <kbd>Enter</kbd>/<kbd>Space</kbd> select, <kbd>*</kbd> expands every sibling.',
        'Disabled nodes stay focusable (announced via <code>aria-disabled</code>) but can never be selected. Rows and the disclosure toggle meet the 44px touch target; <code>:focus-visible</code> draws the shared focus ring.',
      ],
      api: {
        nodes:
          'The full hierarchy to render. Each node: { id, label, children?, icon?, disabled?, data? }.',
        selectionMode: 'none, single (default) or multiple. single keeps at most one id.',
        selectedIds: 'Two-way array of selected node ids.',
        expandedIds:
          'Two-way array of expanded parent ids — the tree derives the visible rows from it.',
        expandOnSelect: 'Clicking a parent row also toggles its expansion (not only the chevron).',
        showGuides: 'Draw connector guide lines along each level’s indentation.',
        ariaLabel: 'Accessible label for the role="tree" element.',
        ariaLabelledby: 'Id of an external element that labels the tree.',
        nodeSelect: 'Emits the node whenever it becomes selected (or toggled in multiple).',
        nodeToggle: 'Emits the node whenever its expanded state is toggled.',
        nodeTemplate:
          'Projected template replacing the default label of every row (context: node, level, expanded, selected); chevron, ARIA and keyboard are kept.',
        expandAll: 'Expands every parent node (call it on a template ref / viewChild).',
        collapseAll: 'Collapses every node.',
      },
      labels: {
        filesAria: 'Project files',
        settingsAria: 'Workspace settings',
        fileExplorer: 'File explorer (single selection)',
        multiple: 'Multiple selection',
        controlled: 'Controlled expansion',
        guides: 'Guides on / off',
        disabled: 'Disabled nodes',
        keyboard: 'Keyboard navigation',
        selectedPrefix: 'Selected',
        nothingSelected: 'No node selected',
        expandAll: 'Expand all',
        collapseAll: 'Collapse all',
        expandedUnit: 'expanded',
        withoutGuides: 'Without guides',
        withGuides: 'With guides',
        disabledNote: 'Locked items stay focusable but can never be selected.',
        keyboardHint: 'Click a node first, then try the keys:',
        kbUpDown: 'Move focus between visible nodes',
        kbRight: 'Expand a collapsed parent, then move to its first child',
        kbLeft: 'Collapse an expanded parent, then move to the parent',
        kbHomeEnd: 'Jump to the first / last visible node',
        kbEnterSpace: 'Select (and toggle a parent)',
        kbStar: 'Expand every sibling at the current level',
        compositionTitle: 'Access control',
        compositionSubtitle:
          'Pick departments, teams or people and grant them access to this project.',
        orgLabel: 'Organization',
        peopleUnit: 'people',
        selectedSuffix: 'selected',
        clear: 'Clear',
        typeDepartment: 'Department',
        typeTeam: 'Team',
        typePerson: 'Person',
        grantAccess: 'Grant access',
        grantDesc: 'Selected members can view and edit this project.',
        save: 'Save changes',
        saving: 'Saving…',
        saved: 'Saved!',
        emptyDetail: 'Select a department, team or person on the left to see its details.',
      },
    },
    timeline: {
      lead: 'Chronological sequence of events along a rail: a marker per event (dot, glyph or any projected element), a connector to the next one and a title / time / body block. Vertical by default, <code>alternate</code> zig-zag layout and a horizontal mode. Semantically a plain list — markers and connectors are decorative. No Angular Material dependency.',
      apiCaption: 'dm-timeline / dm-timeline-item inputs and the dmTimelineMarker directive',
      defaultsDesc:
        'Change the defaults of every timeline with the <code>TIMELINE_DEFAULTS</code> token or the <code>provideTimelineDefaults</code> helper — <code>orientation</code>, <code>align</code>, <code>size</code>, <code>color</code> and the item <code>variant</code>.',
      a11yItems: [
        'Container is <code>role="list"</code>; every <code>dm-timeline-item</code> is <code>role="listitem"</code>. Give the list an <code>ariaLabel</code> when several timelines share a page.',
        'Pass <code>datetime</code> (ISO 8601) so the stamp renders as a real <code>&lt;time datetime&gt;</code> with a machine-readable value.',
        'Default markers, state glyphs and connectors are <code>aria-hidden</code>; state is conveyed by the visible title / time / body, never by color alone. A projected custom marker keeps its own semantics.',
        'The <code>active</code> ring pulse is driven by <code>--dm-timeline-pulse</code> and honors <code>prefers-reduced-motion</code>.',
      ],
      api: {
        orientation: 'Layout direction: vertical rail or one column per item.',
        align:
          'Content placement (vertical only). alternate centers the rail and zig-zags from the md breakpoint up.',
        size: 'Marker diameter, spacing and type scale.',
        color: 'Default accent of the markers; each item may override it.',
        ariaLabel: 'Accessible label for the role="list" host.',
        itemCount: 'Read-only number of registered items.',
        title: 'Event title, rendered as <strong>. Stripped from the host (no native tooltip).',
        time: 'Human-readable stamp ("2 hours ago", "Mar 5, 2026").',
        datetime: 'ISO 8601 value; when set the stamp renders as <time [datetime]>.',
        itemColor: 'Per-item accent override. Falls back to the timeline color.',
        variant: 'solid accent fill, or outlined: surface fill with an accent ring.',
        state:
          'active pulses a ring, completed shows a check and accents the connector, error shows a warning glyph in danger.',
        index: 'Read-only zero-based position of the item.',
        isLast: 'Read-only; true for the last item (no trailing connector).',
        marker:
          'Put it on a projected element (dm-avatar, dm-icon, a span…) to replace the default dot with it.',
      },
      labels: {
        orderAria: 'Order history',
        placed: 'Order placed',
        placedBody: 'We received your order and sent a confirmation email.',
        packed: 'Packed',
        packedBody: 'Your items were packed at our Madrid warehouse.',
        shipped: 'Shipped',
        shippedBody: 'Handed to the carrier. Tracking number ES-7731-4821.',
        outForDelivery: 'Out for delivery',
        outForDeliveryBody: 'The courier is on the way — expect a call.',
        delivered: 'Delivered',
        deliveredBody: 'Left with the concierge.',
        pipelineAria: 'Pipeline run #512',
        checkout: 'Checkout',
        build: 'Build',
        buildBody: 'Compiled in 42 s.',
        lint: 'Lint',
        lintBody: '3 warnings promoted to errors.',
        tests: 'Tests',
        testsBody: 'Running 148 specs…',
        deploy: 'Deploy',
        deployBody: 'Waiting for the previous jobs.',
        outlined: 'Outlined markers & per-item colors',
        incidentAria: 'Incident #2093',
        incidentOpened: 'Incident opened',
        incidentOpenedBody: 'Error rate above 5 % on the checkout API.',
        investigating: 'Investigating',
        investigatingBody: 'On-call engineer paged; rollback under evaluation.',
        fixDeployed: 'Fix deployed',
        fixDeployedBody: 'Hotfix 2.4.1 rolled out to every region.',
        resolved: 'Resolved',
        resolvedBody: 'Error rate back to baseline. Post-mortem scheduled.',
        customMarkers: 'Custom markers',
        feedAria: 'Activity feed',
        anaCommented: 'Ana commented',
        anaBody: 'Looks good to me — ship it!',
        deploySucceeded: 'Deploy succeeded',
        deploySucceededBody: 'Preview environment is live.',
        lucasPushed: 'Lucas pushed 3 commits',
        starred: 'New star',
        starredBody: 'The repository reached 1 000 stars.',
        timeAgo2h: '2 hours ago',
        timeAgo4h: '4 hours ago',
        timeYesterday: 'Yesterday',
        timeAgo3d: '3 days ago',
        alternate: 'Alternate alignment',
        milestonesAria: 'Project milestones',
        v1: 'v1.0 — First release',
        v1Time: 'Jan 2025',
        v1Body: 'Buttons, forms and overlays.',
        v15: 'v1.5 — Dark mode',
        v15Time: 'Jun 2025',
        v15Body: 'Semantic tokens across both themes.',
        v2: 'v2.0 — Data display',
        v2Time: 'Mar 2026',
        v2Body: 'Table, tree and timeline.',
        v3: 'v3.0 — Signal Forms',
        v3Body: 'Planned for next year.',
        horizontal: 'Horizontal',
        roadmapAria: 'Release roadmap',
        plan: 'Plan',
        planBody: 'Scope and design.',
        buildPhase: 'Build',
        buildPhaseBody: 'Core components.',
        beta: 'Beta',
        betaBody: 'Early adopters testing.',
        ga: 'GA',
        gaBody: 'Public release.',
        q1: 'Q1 2026',
        q2: 'Q2 2026',
        q3: 'Q3 2026',
        q4: 'Q4 2026',
        orderTitle: 'Order #4821',
        orderSubtitle: '3 items · Ships to Madrid',
        statusProcessing: 'Processing',
        statusShipped: 'Shipped',
        statusOutForDelivery: 'Out for delivery',
        statusDelivered: 'Delivered',
        etaPrefix: 'Estimated delivery',
        deliveredPrefix: 'Delivered on',
        pending: 'Pending',
        advance: 'Advance',
        track: 'Track package',
        reset: 'Reset',
      },
    },
    divider: {
      lead: 'Thin rule that separates content — horizontal or vertical, with an optional inline label. Pure theming: semantic tokens in both themes, never a hardcoded color.',
      apiCaption: 'dm-divider inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-divider</code> with the <code>DIVIDER_DEFAULTS</code> token or the <code>provideDividerDefaults</code> helper:',
      a11yItems: [
        'Host is <code>role="separator"</code> with <code>aria-orientation</code> reflecting the orientation, so assistive tech announces it as a divider.',
        'The projected label is presentational text inside the separator — read inline, with no extra semantics.',
        'Colors come from the semantic tokens, tuned for enough contrast in both light and dark themes.',
      ],
      api: {
        orientation: 'Line direction. vertical stretches to the parent’s cross size.',
        labelPlacement: 'Where the projected label sits along the line: start, center, end.',
        content:
          'Projected label. With content the line splits around it; empty renders a single continuous rule.',
        lineStyle:
          'Stroke style of the line: solid, dashed or dotted. Non-solid lines are drawn as a border reusing the color and thickness tokens.',
      },
      labels: { vertical: 'Vertical' },
    },
    progress: {
      lead: 'Linear progress bar with an optional label row, determinate and indeterminate modes, semantic colors and an optional striped fill. Pill track, zero external dependencies.',
      apiCaption: 'dm-progress inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-progress</code> with the <code>PROGRESS_DEFAULTS</code> token or the <code>provideProgressDefaults</code> helper:',
      a11yItems: [
        'Host is <code>role="progressbar"</code> with <code>aria-valuemin</code> / <code>aria-valuemax</code> / <code>aria-valuenow</code>; the accessible name falls back from <code>ariaLabel</code> to <code>label</code>.',
        'While indeterminate, <code>aria-valuenow</code> and <code>aria-valuetext</code> are omitted per the ARIA spec; otherwise <code>aria-valuetext</code> mirrors the formatted value, so custom formats are announced as written.',
        'With <code>prefers-reduced-motion: reduce</code> the indeterminate sweep stops and renders as a static tinted track.',
      ],
      api: {
        value: 'Current value, clamped to [0, max]. null renders the indeterminate sweep.',
        max: 'Upper bound of the range (the lower bound is always 0).',
        color: 'Semantic color of the fill.',
        size: 'Track thickness: sm, md, lg.',
        label: 'Text shown above the bar.',
        showValueLabel: 'Shows the formatted value at the end of the label row.',
        formatValue: 'Formats the value label. Defaults to a rounded percentage (73%).',
        striped: 'Overlays diagonal stripes on the fill (theme-derived, no hard colors).',
        ariaLabel: 'Accessible name. Falls back to label.',
      },
      labels: {
        colors: 'Colors',
        indeterminate: 'Indeterminate',
        striped: 'Striped',
        formatValue: 'Custom value format',
      },
    },
    alert: {
      lead: 'Contextual feedback banner with a color × variant API, a semantic icon per color, an optional dismiss button and an action slot.',
      apiCaption: 'dm-alert inputs and outputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-alert</code> — including the dismiss button label — with the <code>ALERT_DEFAULTS</code> token or the <code>provideAlertDefaults</code> helper:',
      a11yItems: [
        'Host is <code>role="alert"</code>: the content is announced assertively as soon as it appears.',
        'The dismiss button is a real <code>&lt;button&gt;</code> with an accessible label and a ≥44px touch target; pressing it hides the alert and emits <code>closed</code>.',
        'Each color ships a semantic icon as a redundant cue beyond color; hide it with <code>hideIcon</code> when the copy already carries the meaning.',
      ],
      api: {
        color: 'Semantic color. It also selects the built-in icon.',
        variant: 'Visual variant: flat, faded, solid, bordered.',
        title: 'Bold first line of the alert.',
        description: 'Supporting text under the title. Free content can also be projected.',
        hideIcon: 'Hides the semantic icon.',
        dismissible: 'Shows the dismiss button; the alert hides itself when pressed.',
        iconSlot:
          'Projected custom icon: replaces the built-in tone glyph inside the icon box (import DmAlertIconDirective). hideIcon still hides it.',
        dismissLabel: 'Accessible label of the dismiss button (localize it via the defaults).',
        closed: 'Emitted after the alert is dismissed.',
      },
      labels: {
        show: 'Show alert',
        colors: 'Colors',
        description: 'Long description & custom body',
        dismissible: 'Dismissible',
        action: 'Action slot',
        undo: 'Undo',
      },
    },
    pagination: {
      lead: 'Page navigation with a windowed list of page numbers: boundary pages at the extremes, siblings around the current page and ellipses in between. The current page is two-way bound and every accessible label is overridable.',
      apiCaption: 'dm-pagination inputs and outputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-pagination</code> — including every accessible label, the recommended place to localize them — with the <code>PAGINATION_DEFAULTS</code> token or the <code>providePaginationDefaults</code> helper:',
      a11yItems: [
        'Semantic structure: a <code>&lt;nav&gt;</code> landmark (named by <code>ariaLabel</code>) wrapping a real <code>&lt;ul&gt;</code>/<code>&lt;li&gt;</code>/<code>&lt;button&gt;</code> list; the current page carries <code>aria-current="page"</code>.',
        'Every control has a descriptive ARIA label (<code>prevLabel</code>, <code>nextLabel</code>, <code>pageAriaLabel</code>), overridable per instance or via <code>providePaginationDefaults()</code>; ellipses are presentational (<code>aria-hidden</code>).',
        'Touch targets stay ≥44px and the elastic press honors reduced-motion; below the sm breakpoint off-window pages collapse, but the first, last and current pages always stay reachable.',
      ],
      api: {
        page: 'Two-way current page (1-based). Navigation clamps into range.',
        totalPages: 'Total number of pages. Wins over the length/pageSize derivation.',
        siblingCount: 'Pages shown on each side of the current page.',
        boundaryCount: 'Pages always shown at the start and end.',
        showControls: 'Previous/next chevron buttons (disabled at the extremes).',
        size: 'Item size — 32 / 40 / 48px.',
        color: 'Semantic color of the active page (solid fill).',
        disabled: 'Disables every control.',
        ariaLabel: 'Accessible name of the nav landmark.',
        prevLabel: 'ARIA label of the previous-page button.',
        nextLabel: 'ARIA label of the next-page button.',
        pageAriaLabel: 'Builds the ARIA label of each page button.',
        pageChange: 'Emitted with the new page (model).',
        length: 'Total item count; combined with pageSize it derives the page count.',
        pageSize: 'Items per page; combined with length it derives the page count.',
      },
      labels: {
        currentPage: 'Page',
        window: 'Siblings & boundaries',
        colors: 'Colors',
        noControls: 'Numbers only',
        disabled: 'Disabled',
        articlesTitle: 'Articles',
        showing: 'Showing',
        of: 'of',
      },
    },
    slider: {
      lead: 'Single-value slider (<code>role="slider"</code>). Drag with the pointer, click the track to jump, or use the full keyboard map. Works standalone with <code>[(value)]</code> and with Angular forms via <code>ControlValueAccessor</code>.',
      apiCaption: 'dm-slider inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-slider</code> with the <code>SLIDER_DEFAULTS</code> token or the <code>provideSliderDefaults</code> helper:',
      a11yItems: [
        'The thumb is <code>role="slider"</code> with <code>aria-valuemin</code> / <code>aria-valuemax</code> / <code>aria-valuenow</code> and <code>aria-valuetext</code> (the formatted value).',
        'Full keyboard: Arrow keys step, PageUp/PageDown jump, Home/End go to the extremes; name it with <code>ariaLabel</code> or an external form label.',
        'Disabled exposes <code>aria-disabled="true"</code> and leaves the tab order; the thumb motion honors reduced-motion.',
      ],
      api: {
        value: 'Two-way current value: [(value)] / (valueChange).',
        min: 'Lower bound of the range.',
        max: 'Upper bound of the range.',
        step: 'Increment between valid values.',
        size: 'Track thickness: sm, md, lg.',
        color: 'Semantic color of the filled track and thumb.',
        disabled: 'Combined with the forms disabled state.',
        showValueLabel: 'Shows a value bubble above the thumb.',
        formatValue: 'Formats the value bubble and aria-valuetext.',
        marks: 'Tick marks along the track: { value, label? }[].',
        ariaLabel: 'Accessible name when there is no external label.',
      },
      labels: {
        marks: 'Marks & steps',
        currency: 'Custom value format',
        compositionTitle: 'Quick settings',
        compositionDesc: 'Changes apply instantly',
        brightness: 'Brightness',
        brightnessDesc: 'Screen brightness',
        notifVolume: 'Notifications',
        notifVolumeDesc: 'Alert volume',
        textSize: 'Text size',
        textSizeDesc: 'Base font size',
      },
    },
    rating: {
      lead: 'Star rating (<code>role="slider"</code>). Hover to preview, click to commit, or drive it from the keyboard. Half-star precision, a <code>readonly</code> mode that renders fractional averages (<code>3.7</code>) and a custom glyph. Works standalone with <code>[(value)]</code> and with Angular forms via <code>ControlValueAccessor</code>.',
      apiCaption: 'dm-rating inputs & outputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-rating</code> with the <code>RATING_DEFAULTS</code> token or the <code>provideRatingDefaults</code> helper:',
      a11yItems: [
        'The host is the focusable control: <code>role="slider"</code> with <code>aria-valuemin</code> / <code>aria-valuemax</code> / <code>aria-valuenow</code> and a human <code>aria-valuetext</code> ("3 of 5 stars"); name it with <code>ariaLabel</code> or an external <code>aria-labelledby</code>.',
        'Keyboard: ArrowRight/ArrowUp and ArrowLeft/ArrowDown move one step (<code>0.5</code> with <code>allowHalf</code>), Home clears to 0 and End jumps to <code>max</code>. Clicking the current value again clears the rating.',
        '<code>readonly</code> exposes <code>aria-readonly="true"</code>, <code>disabled</code> exposes <code>aria-disabled="true"</code>; both drop out of the tab order and ignore pointer/keyboard.',
        'Star glyphs are <code>aria-hidden</code> — the value is conveyed entirely through the slider attributes; interactive stars keep a ≥44px touch target and the fill transition honors reduced-motion.',
      ],
      api: {
        value: 'Two-way current rating: [(value)] / (valueChange). Also the forms model.',
        max: 'Number of stars rendered (the maximum value).',
        allowHalf: 'Half-star precision on hover and keyboard (step becomes 0.5).',
        readonly: 'Display-only: renders fractional fills, no interaction.',
        disabled: 'Combined with the forms disabled state.',
        size: 'Star size scale: sm, md, lg.',
        color: 'Semantic color of the filled stars.',
        ariaLabel: 'Accessible name of the rating.',
        character: 'Custom glyph instead of the default star (e.g. ❤).',
        rateChange: 'Emitted when a new rating is committed by pointer or keyboard.',
      },
      labels: {
        halfStars: 'Half stars',
        readonlyAvg: 'Read-only average',
        reviewsCount: '1,284 reviews',
        character: 'Custom character',
        productName: 'Aurora X1 Headphones',
        orderMeta: 'Order #48213 · Delivered Aug 12',
        yourRating: 'Your rating',
        tapToRate: 'Tap a star to rate',
        rate1: 'Poor',
        rate2: 'Fair',
        rate3: 'Good',
        rate4: 'Very good',
        rate5: 'Excellent',
        ratingRequired: 'Please pick a rating.',
        reviewLabel: 'Your review',
        reviewHint: 'Optional',
        reviewPlaceholder: 'What did you like or dislike?',
        submit: 'Post review',
        submitting: 'Posting…',
        submitted: 'Review posted',
      },
    },
    breadcrumbs: {
      lead: 'Composite breadcrumb trail: a <code>&lt;nav&gt;</code> landmark wrapping an ordered list of items, an auto-marked current page and optional middle collapse. No <code>@angular/router</code> dependency — items with <code>href</code> render anchors.',
      apiCaption: 'dm-breadcrumbs / dm-breadcrumb-item inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-breadcrumbs</code> with the <code>BREADCRUMBS_DEFAULTS</code> token or the <code>provideBreadcrumbsDefaults</code> helper:',
      a11yItems: [
        'Renders a <code>&lt;nav&gt;</code> landmark (named by <code>ariaLabel</code>) wrapping an ordered <code>&lt;ol&gt;</code>; the last item is marked <code>aria-current="page"</code>.',
        'Items with <code>href</code> are real anchors (focusable, keyboard-operable); disabled items expose <code>aria-disabled</code> and drop out of the tab order.',
        'When collapsed, the hidden middle is replaced by a presentational <code>…</code> and separators are <code>aria-hidden</code>, so neither is announced.',
      ],
      api: {
        separator: 'Separator between items. Empty falls back to the built-in chevron.',
        size: 'Size scale: sm, md, lg.',
        ariaLabel: 'Accessible label for the nav landmark.',
        maxItems: 'Collapse the trail once it has more than this many items. null disables.',
        itemsBeforeCollapse: 'Items kept at the start when collapsed.',
        itemsAfterCollapse: 'Items kept at the end when collapsed.',
        href: 'Item link. With href the item is an anchor; without it, plain text.',
        disabled: 'Disables an individual item.',
        separatorTemplate:
          'Projected template replacing the built-in chevron (and any separator string) between items. $implicit is the zero-based index of the preceding crumb.',
      },
      labels: {
        separator: 'Custom separator',
        disabled: 'Disabled item',
        collapse: 'Collapsed (maxItems)',
      },
    },
    menu: {
      lead: 'Dropdown menu following the WAI-ARIA menu pattern. A trigger directive portalises the panel into a CDK overlay; the menu owns roving focus, sections, dividers, item shortcuts and a danger color.',
      apiCaption: 'dm-menu / dm-menu-item / dm-menu-section inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-menu</code> with the <code>MENU_DEFAULTS</code> token or the <code>provideMenuDefaults</code> helper:',
      a11yItems: [
        'The panel is <code>role="menu"</code> and every item is a <code>role="menuitem"</code> button; the trigger exposes <code>aria-haspopup="menu"</code>, <code>aria-expanded</code> and <code>aria-controls</code>.',
        'Roving focus via the CDK <code>FocusKeyManager</code>: Arrow keys move, Home/End jump, ArrowDown/Enter open on the first item and ArrowUp on the last.',
        'Escape closes the menu and returns focus to the trigger; disabled items are skipped by the keyboard and cannot be activated.',
      ],
      api: {
        trigger: 'Directive that wires the trigger to a #menu template reference.',
        placement: 'Preferred panel placement; flips automatically when space is tight.',
        closeOnSelect: 'Close the menu when an item is activated.',
        ariaLabel: 'Accessible name of the menu panel.',
        events: 'opened / closed lifecycle notifications.',
        itemDisabled: 'Disables an individual item.',
        itemColor: 'Item color: default or danger.',
        shortcut: 'Shortcut hint shown at the end of the item.',
        selected: 'Emitted when the item is activated.',
        sectionHeading: 'Heading of a grouped section.',
      },
      labels: {
        lastAction: 'Last action',
        icons: 'Icons & shortcuts',
        sections: 'Sections & divider',
        danger: 'Danger item',
        persistent: 'Stay open (closeOnSelect)',
        userMenu: 'Account menu',
      },
    },
    popover: {
      lead: 'Floating panel anchored to a trigger, built on the CDK Overlay. Closes on outside click and Escape, flips when space is tight, supports an arrow and an optional focus trap for interactive content.',
      apiCaption: 'dm-popover inputs and outputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-popover</code> with the <code>POPOVER_DEFAULTS</code> token or the <code>providePopoverDefaults</code> helper:',
      a11yItems: [
        'The panel is <code>role="dialog"</code>; the trigger exposes <code>aria-haspopup="dialog"</code>, <code>aria-expanded</code> and <code>aria-controls</code> pointing at it.',
        'Closes on outside click and Escape and returns focus to the trigger; set <code>trapFocus</code> for panels with inputs or buttons so Tab stays inside.',
        'Name the panel with <code>ariaLabel</code>; the arrow is decorative (<code>aria-hidden</code>).',
      ],
      api: {
        placement:
          'Preferred placement; flips automatically. top/bottom/left/right (+ -start/-end).',
        showArrow: 'Shows the pointer arrow.',
        offset: 'Gap in px between the trigger and the panel.',
        trapFocus: 'Traps Tab focus inside the panel (for interactive content).',
        ariaLabel: 'Accessible name of the panel.',
        opened: 'Emitted when the popover opens.',
        closed: 'Emitted when the popover closes.',
        dmPopoverTrigger: 'Directive that wires the trigger to a #popover template reference.',
        open: 'Open state of the panel. Two-way ([(open)]).',
        openChange: 'Emitted when the open state changes (the [(open)] model).',
      },
      labels: {
        trigger: 'Account details',
        trapFocus: 'Trap focus (interactive)',
        hoverCardText: 'Reviewed and approved by',
        hoverCardWhen: '2 hours ago',
        hoverCardBio: 'Mathematician. Wrote the first algorithm meant to run on a machine.',
        follow: 'Follow',
        following: 'Following',
        followers: 'followers',
        followingCount: 'following',
      },
    },
    drawer: {
      lead: 'Slide-in side panel served over the CDK Dialog with the library look. Focus trap, focus restore and Escape/backdrop handling come from the CDK; the panel pins to any viewport edge in four sizes.',
      apiCaption: 'DmDrawerConfig options',
      defaultsDesc:
        'Change the defaults of every drawer with the <code>DRAWER_DEFAULTS</code> token or the <code>provideDrawerDefaults</code> helper:',
      a11yItems: [
        'The panel is <code>role="dialog"</code>; it claims <code>aria-modal="true"</code> only when a backdrop dims the page (a backdrop-less drawer leaves the rest interactive).',
        'Focus is trapped inside while open and restored to the opener on close (CDK); name it with <code>ariaLabel</code>.',
        'Escape and a backdrop click close it unless <code>disableClose</code> is set.',
      ],
      api: {
        data: 'Injected into the content component via DIALOG_DATA.',
        placement: 'Edge the drawer slides from: left, right, top, bottom.',
        size: 'Panel size: sm, md, lg, full.',
        backdrop: 'Dims the page and makes the drawer modal.',
        disableClose: 'Blocks Escape and backdrop close.',
        ariaLabel: 'Accessible name of the drawer.',
      },
      labels: {
        open: 'Open drawer',
        result: 'Last result',
        title: 'Settings',
        notifications: 'Email notifications',
        compact: 'Compact layout',
        save: 'Save',
        close: 'Close',
        left: 'Left',
        right: 'Right',
        top: 'Top',
        bottom: 'Bottom',
        sm: 'Small',
        md: 'Medium',
        lg: 'Large',
        full: 'Full',
        persistent: 'Persistent (disableClose)',
        openPersistent: 'Open persistent',
        cartTitle: 'Your cart',
        cartProduct: 'Design system license',
        cartProductMeta: 'Unlimited projects · 1 seat',
        addToCart: 'Add to cart',
        subtotal: 'Subtotal',
        shippingNote: 'Taxes and shipping calculated at checkout.',
        checkout: 'Checkout',
        continueShopping: 'Continue shopping',
        checkoutDone: 'checkout started',
        cartKept: 'cart kept',
      },
    },
    kbd: {
      lead: 'Keyboard keys drawn like physical key caps. Named keys render their canonical symbol (<code>command</code> → ⌘); letters and digits come through content projection.',
      apiCaption: 'dm-kbd inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-kbd</code> with the <code>KBD_DEFAULTS</code> token or the <code>provideKbdDefaults</code> helper:',
      a11yItems: [
        'Each named key is a real <code>&lt;kbd&gt;</code> element with an <code>aria-label</code> (Command, Shift…); its symbol glyph is <code>aria-hidden</code>.',
        'Projected letters and words are read verbatim by screen readers.',
        'Non-interactive: no focus ring and no motion.',
      ],
      api: {
        keys: 'Named key(s) rendered as their keyboard symbol, in order.',
        size: 'Size scale.',
      },
      labels: {
        single: 'Symbol keys',
        combos: 'Combinations',
        letters: 'Projected letters',
        inline: 'Inline in prose & menus',
      },
    },
    icon: {
      lead: 'Draw an icon three ways: type a font-icon name as the content (<code>&lt;dm-icon&gt;home&lt;/dm-icon&gt;</code>, thousands of icons), reference a registered SVG by <code>name</code>, or project your own <code>&lt;svg&gt;</code>. Inherits <code>currentColor</code> and scales to any size.',
      apiCaption: 'dm-icon inputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-icon</code> with the <code>ICON_DEFAULTS</code> token or the <code>provideIconDefaults</code> helper:',
      a11yItems: [
        'Decorative by default — with no <code>label</code> the host is <code>aria-hidden</code>, so screen readers skip it (icons usually sit next to real text).',
        'Give a <code>label</code> to a meaningful stand-alone icon: the host becomes <code>role="img"</code> with an <code>aria-label</code>.',
        'Color comes from <code>color</code> or the inherited <code>currentColor</code>, so contrast follows the surrounding text.',
        'Spinning icons hold still under <code>prefers-reduced-motion</code>.',
      ],
      api: {
        name: 'Registered SVG icon name (see provideDmasterIcons). For font icons, use the content instead.',
        size: 'Named size (sm/md/lg), a number in pixels, or any CSS length.',
        color:
          'Semantic token (primary, success, …) or any CSS color. Empty inherits currentColor.',
        fill: 'Font mode: filled (true) vs outlined (false). The FILL axis of the variable font.',
        weight: 'Font mode: stroke weight, 100–700. The wght axis.',
        family: 'Font mode family: outlined (default), rounded or sharp.',
        spin: 'Rotate continuously — for loaders. Off under reduced motion.',
        label: 'Accessible name. Empty means decorative (aria-hidden).',
      },
      labels: {
        font: 'Icon font — thousands of icons',
        fontDesc:
          'Put any <a href="https://fonts.google.com/icons" target="_blank" rel="noreferrer">icon-font</a> name as the content and it renders as a ligature. Load the font once (a single <code>&lt;link&gt;</code>) and you get ~3,500 icons for free.',
        styles: 'Fill, weight & family',
        gallery: 'The curated SVG set',
        color: 'Color',
        spin: 'Spinning',
        projected: 'Project your own SVG',
        register: 'Register SVG icons',
        setDesc:
          'The <code>@dmaster/ui/icons</code> entry point ships this hand-drawn outline set — offline, zero-config, and consistent with the components. Register it once and draw any of them by <code>name</code>.',
        registerDesc:
          'Register the curated set — or your own SVG strings — with <code>provideDmasterIcons</code>. It is callable many times, so you can add icons from anywhere.',
        metaFont: '~3,500 font icons',
        metaSvg: '53 curated SVGs',
        metaSsr: 'SSR-safe',
        metaTreeshake: 'Tree-shakeable',
        waysTitle: 'Three ways to draw an icon',
        waysFontTitle: 'Font ligature',
        waysFontBody:
          'Type a name, get one of ~3,500 icons. Best for broad coverage and rapid prototyping — costs a font request, adds no bundle weight.',
        waysSvgTitle: 'Registered SVG',
        waysSvgBody:
          'Offline, zero-config, pixel-consistent with the rest of the library. Best for a stable, on-brand set shipped with your app.',
        waysProjectedTitle: 'Projected SVG',
        waysProjectedBody:
          'Drop in any raw <svg> — a one-off mark, or an icon outside both sets. The escape hatch.',
        searchFont: 'Search font icons…',
        searchSvg: 'Search the curated set…',
        noResults: 'No icons match your search.',
        clickToCopy: 'Click any icon to copy its snippet.',
        copied: 'Copied!',
        fillOutlined: 'Outlined',
        fillFilled: 'Filled',
        familyOutlined: 'Outlined',
        familyRounded: 'Rounded',
        familySharp: 'Sharp',
        stylesOutlined: 'Outlined (default)',
        stylesFilled: 'Filled',
        stylesRounded: 'Rounded',
        stylesSharp: 'Sharp',
        stylesBold: 'Bold (700)',
        catGeneral: 'General',
        catCommunication: 'Communication',
        catPeople: 'People & account',
        catNavigation: 'Navigation & UI',
        catFiles: 'Files & media',
        catStatus: 'Status & feedback',
        catPlaces: 'Time, place & commerce',
        catDev: 'Developer',
      },
    },
    command: {
      lead: 'A ⌘K command palette — a centered modal with fuzzy search over grouped actions. Declared once, driven by a two-way <code>open</code> model and a global hotkey.',
      apiCaption: 'Inputs, outputs and item fields for dm-command.',
      defaultsDesc:
        'Override the hotkey and copy app-wide with <code>provideCommandDefaults()</code>.',
      a11yItems: [
        'The panel is <code>role="dialog"</code> <code>aria-modal="true"</code>; focus is trapped and restored to the previously-focused element on close.',
        'The input is <code>role="combobox"</code> with <code>aria-expanded</code>, <code>aria-controls</code> and <code>aria-activedescendant</code> — focus stays in the input while the active row moves.',
        'The list is <code>role="listbox"</code>; rows are <code>role="option"</code> with <code>aria-selected</code>; groups are <code>role="group"</code> labelled by their heading.',
        'Full keyboard: type to filter, ↑/↓ to move (wrapping, skipping disabled), Home/End, ↵ to select, Esc to close.',
        'The hotkey (<code>mod+k</code>) toggles from anywhere; <code>mod</code> resolves to ⌘ on macOS and Ctrl elsewhere. The listener is browser-only (SSR-safe).',
      ],
      api: {
        items: 'Actions to render (DmCommandItem[]). Required.',
        open: 'Two-way open state of the palette.',
        hotkey: "Global toggle combo; 'mod' = ⌘/Ctrl. Empty string disables it.",
        itemTemplate:
          'Projected template replacing each row’s content — label + shortcut chip (context: item, active). Selection, highlight and ARIA stay with the palette.',
        placeholder: 'Placeholder shown in the search input.',
        emptyLabel: 'Message shown when the query matches nothing.',
        ariaLabel: 'Accessible name for the modal dialog.',
        selected: 'Emitted when an enabled item is activated; the palette then closes.',
        shortcut: 'Per-item shortcut hint rendered on the right (e.g. ⌘P).',
        group: 'Per-item group heading; items sharing a group render together.',
        keywords: 'Extra terms matched by the search on top of the label.',
        itemDisabled: 'Disabled items are skipped by the keyboard and not selectable.',
      },
      labels: {
        open: 'Open palette',
        lastAction: 'Last action',
        shortcuts: 'With per-item shortcuts',
        empty: 'Custom empty state',
        emptyLabel: 'Nothing matches — try another term',
        searchPlaceholder: 'Search…',
        groupNavigation: 'Navigation',
        groupActions: 'Actions',
        groupTheme: 'Theme',
        navDashboard: 'Dashboard',
        navProjects: 'Projects',
        navSettings: 'Settings',
        actionNewProject: 'New project',
        actionInvite: 'Invite member',
        themeLight: 'Light',
        themeDark: 'Dark',
      },
    },
    stepper: {
      lead: 'Guides the user through an ordered sequence of steps, one panel at a time. Horizontal or vertical, optional <code>linear</code> gating, per-step <code>completed</code> / <code>error</code> / <code>optional</code> / <code>disabled</code> states and semantic colors. It is a navigational list of steps — not a tablist — with full keyboard support and zero Angular Material.',
      apiCaption: 'dm-stepper / dm-step inputs, outputs and methods',
      defaultsDesc:
        'Change the defaults with the <code>STEPPER_DEFAULTS</code> token or the <code>provideStepperDefaults</code> helper.',
      a11yItems: [
        'The container is <code>role="list"</code>; each step is <code>role="listitem"</code> whose header is a real <code>&lt;button&gt;</code>. The active header carries <code>aria-current="step"</code>.',
        'Each header wires <code>aria-controls</code> to its <code>role="region"</code> panel, which is <code>aria-labelledby</code> the header — panels are hidden with the native <code>hidden</code> attribute while inactive.',
        'Arrow keys (<kbd>←</kbd>/<kbd>→</kbd>, or <kbd>↑</kbd>/<kbd>↓</kbd> when vertical) move focus between headers; <kbd>Home</kbd>/<kbd>End</kbd> jump to the first/last enabled step; <kbd>Enter</kbd>/<kbd>Space</kbd> activate.',
        '<code>disabled</code> steps use the native disabled button and are skipped by keyboard navigation; steps locked by <code>linear</code> expose <code>aria-disabled="true"</code> while staying focusable.',
        'Indicator glyphs (number, check, warning) are <code>aria-hidden</code>; state is conveyed by the visible label. Motion honors <code>prefers-reduced-motion</code>.',
      ],
      api: {
        activeStep: 'Zero-based index of the active step (two-way: [(activeStep)]).',
        orientation: 'Layout direction: horizontal (default) or vertical.',
        linear:
          'A step further ahead is selectable only once every step before it is completed. Going back is always allowed.',
        color: 'Semantic accent of the active / completed indicators.',
        size: 'Indicator diameter + type scale (sm, md, lg).',
        ariaLabel: 'Accessible label for the step list.',
        stepCount: 'Number of registered <dm-step> children (read-only signal).',
        next: 'Advances to the next reachable step, if any.',
        previous: 'Returns to the previous enabled step, if any.',
        select: 'Activates the step at index (no-op if disabled or unreachable).',
        label: 'Short label shown in the step header.',
        optional: 'Marks the step as optional; renders the optionalLabel hint.',
        optionalLabel: 'Copy for the optional hint (localise from the consumer).',
        completed: 'Shows the check glyph; unlocks later steps in linear mode.',
        error: 'Flags the step as errored (warning glyph, danger accent).',
        disabled: 'Not selectable; keyboard navigation skips it.',
        indicatorTemplate:
          "Projected ng-template replacing the indicator's inner content (number / check / error glyph) in every step header. Context: index, active, completed, error.",
      },
      labels: {
        vertical: 'Vertical',
        linear: 'Linear mode (Next / Back)',
        colors: 'Colors',
        optionalHint: 'Optional',
        checkoutAria: 'Checkout steps',
        trackingAria: 'Order tracking',
        linearAria: 'Onboarding steps',
        statesAria: 'Step states',
        stepAccount: 'Account',
        stepShipping: 'Shipping',
        stepPayment: 'Payment',
        stepReview: 'Review',
        accountBody: 'Create your account or sign in to continue.',
        shippingBody: 'Where should we send your order?',
        paymentBody: 'Choose how you want to pay.',
        orderPlaced: 'Order placed',
        orderPlacedBody: 'We received your order and sent a confirmation email.',
        outForDelivery: 'Out for delivery',
        outForDeliveryBody: 'The courier picked up your parcel this morning.',
        delivered: 'Delivered',
        deliveredBody: 'Left with the concierge. Enjoy!',
        stepProfile: 'Profile',
        stepPreferences: 'Preferences',
        linearProfileBody:
          'Tell us a bit about yourself. The next step unlocks once this one is marked complete — try clicking “Review” before that.',
        linearPrefsBody: 'Pick your notification and privacy preferences.',
        linearReviewBody:
          'Everything looks good — you can jump back to any previous step at any time.',
        markDone: 'Mark this step as complete',
        back: 'Back',
        next: 'Next',
        completedStep: 'Completed',
        errorStep: 'Needs attention',
        activeStep: 'In progress',
        optionalStep: 'Optional step',
        disabledStep: 'Unavailable',
        statesBody:
          'Each header reflects its state: a check glyph when completed, a warning glyph on error, a hint under optional steps and a native disabled button when unavailable.',
        checkoutTitle: 'Checkout',
        checkoutSubtitle: 'Three quick steps — your cart is saved.',
        fullName: 'Full name',
        nameRequired: 'Your name is required.',
        email: 'Email',
        emailRequired: 'Email is required.',
        emailInvalid: 'Enter a valid email address.',
        address: 'Street address',
        addressRequired: 'Address is required.',
        city: 'City',
        cityRequired: 'City is required.',
        country: 'Country',
        countryPlaceholder: 'Select a country',
        countryRequired: 'Pick a country.',
        countryEs: 'Spain',
        countryFr: 'France',
        countryDe: 'Germany',
        countryIt: 'Italy',
        paymentMethod: 'Payment method',
        paymentPlaceholder: 'Choose a method',
        paymentRequired: 'Choose a payment method.',
        payCard: 'Credit card',
        payPaypal: 'PayPal',
        payTransfer: 'Bank transfer',
        acceptTerms: 'I agree to the terms and the refund policy.',
        termsRequired: 'You must accept the terms to continue.',
        summaryShipTo: 'Ship to',
        summaryPayWith: 'Pay with',
        summaryTotal: 'Total',
        placeOrder: 'Place order',
        placing: 'Placing order…',
        placed: 'Order placed!',
      },
    },
    fileUpload: {
      lead: 'A drag-and-drop dropzone backed by a hidden native <code>&lt;input type="file"&gt;</code>: selected-file list, image thumbnails, optional per-file progress bars and built-in validation against <code>accept</code>, <code>maxSize</code> and <code>maxFiles</code>. It is <strong>controlled</strong> through a two-way <code>[(files)]</code> model (File objects don’t fit <code>ControlValueAccessor</code> cleanly) — bridge it to a <code>FormControl&lt;File[]&gt;</code> in two lines when you need Reactive Forms. No Angular Material dependency.',
      apiCaption: 'dm-file-upload inputs, outputs & helpers',
      defaultsDesc:
        'Change the defaults of every <code>dm-file-upload</code> (<code>multiple</code>, <code>maxSize</code>, <code>maxFiles</code>) with the <code>FILE_UPLOAD_DEFAULTS</code> token or the <code>provideFileUploadDefaults</code> helper:',
      a11yItems: [
        'The dropzone is a real <code>&lt;button&gt;</code>: <strong>Enter</strong> / <strong>Space</strong> open the native picker, so drag-and-drop is never the only way in. Pass <code>ariaLabel</code> to override the visible copy as the accessible name.',
        'The native <code>&lt;input type="file"&gt;</code> (with <code>accept</code> / <code>multiple</code>) is kept in the DOM but out of the tab order and hidden from assistive tech — the button is the single focus stop.',
        'Validation failures surface in an inline <code>role="alert"</code> (localise its copy through <code>messages</code>) and are also emitted as <code>(fileRejected)</code> so you can toast or log them.',
        'The selected-file list is a <code>&lt;ul&gt;</code>; every remove control is a <code>&lt;button&gt;</code> labelled <code>"&lt;removeLabel&gt; &lt;filename&gt;"</code>.',
        'Per-file progress renders <code>role="progressbar"</code> with <code>aria-valuemin/max/now</code> and the file name as its label.',
        'The hint is wired to the dropzone via <code>aria-describedby</code>; <code>:focus-visible</code> shows the shared focus ring; touch targets meet 44×44px; motion is disabled under <code>prefers-reduced-motion</code>.',
      ],
      api: {
        files: 'Two-way accepted-files model: [(files)] / (filesChange).',
        multiple:
          'Allow more than one file (bare attribute). Single mode replaces the previous pick.',
        accept: 'Native accept filter (e.g. image/*,.pdf) — also enforced during validation.',
        maxSize: 'Maximum size per file, in bytes. null disables the check.',
        maxFiles: 'Maximum number of files kept at once (multiple only). null disables the cap.',
        disabled: 'Disables clicking, dropping and removing.',
        ariaLabel: 'Accessible name for the dropzone button (overrides the visible label).',
        label: 'Dropzone headline copy (the library ships a generic English fallback).',
        hint: 'Sub-text under the headline — ideal for accepted types and size limits.',
        removeLabel: 'Prefix of each remove button’s aria-label ("Remove photo.png").',
        messages: 'Copy for the three rejection reasons: { type, size, count }.',
        progress: 'Per-file progress (0–100) keyed by file name; shows a bar under that row.',
        filesChange: 'Emitted whenever the accepted-files model changes.',
        filesAdded: 'Emitted with the newly accepted files after a successful add.',
        fileRejected: 'Emitted once per refused file: { file, reason: "type" | "size" | "count" }.',
        fileRemoved: 'Emitted with the file removed through its × button.',
        formatFileSize:
          'Pure helper exported from the package — the same byte formatter the list uses ("820 KB", "3.4 MB").',
        dropzoneSlot:
          "Projected element ([dmDropzoneContent]) that replaces the dropzone's built-in icon + label + hint block. Drag & drop, click-to-browse and keyboard behaviour are untouched.",
      },
      labels: {
        dropLabel: 'Drag & drop files here, or click to browse',
        dropLabelSingle: 'Drag & drop a file here, or click to browse',
        dropHint: 'Any file type',
        remove: 'Remove',
        rejectType: 'file type not allowed',
        rejectSize: 'file is too large',
        rejectCount: 'too many files',
        singleHeading: 'Single file',
        selected: 'Selected',
        multipleHeading: 'Multiple files with list',
        anyTypeHint: 'Any type · no size limit',
        filesWord: 'files',
        imagesHeading: 'Images with thumbnails',
        dropImagesLabel: 'Drop images here, or click to browse',
        imagesHint: 'PNG, JPG, GIF, WebP…',
        limitHeading: 'Size limit & rejections',
        limitHint: 'PNG, JPG or PDF · up to',
        maxTwo: 'max 2 files',
        rejectedTitle: 'File rejected',
        dismiss: 'Dismiss',
        limitIdle: 'Try dropping a file over 195 KB, a third file, or a .txt to see the rejection.',
        progressHeading: 'Upload progress',
        progressHint: 'Pick a few files, then press “Simulate upload”.',
        simulateUpload: 'Simulate upload',
        uploading: 'Uploading…',
        uploaded: 'Uploaded',
        reset: 'Reset',
        allDone: 'All files uploaded',
        disabledLabel: 'Uploads are locked',
        disabledHint: 'Contact an admin to unlock this form.',
        ticketTitle: 'Submit a support ticket',
        ticketSubtitle: 'We usually answer within one business day.',
        subjectLabel: 'Subject',
        subjectPlaceholder: 'Short summary of the issue',
        subjectRequired: 'A subject is required.',
        categoryLabel: 'Category',
        categoryPlaceholder: 'Pick a category',
        categoryRequired: 'Pick a category.',
        categoryBug: 'Bug report',
        categoryBilling: 'Billing',
        categoryFeature: 'Feature request',
        categoryOther: 'Something else',
        messageLabel: 'Message',
        messageHint: 'At least 20 characters.',
        messageRequired: 'Describe the problem.',
        messageMinLength: 'Please add a few more details (20+ characters).',
        attachmentsLabel: 'Attachments',
        attachmentsDrop: 'Drop screenshots or a PDF here, or click to browse',
        attachmentsHint: 'PNG, JPG or PDF · up to',
        maxThree: 'max 3 files',
        attachmentsRequired: 'Attach at least one screenshot or document.',
        attachmentsWord: 'attachments',
        sendTicket: 'Send ticket',
        sending: 'Sending…',
        sent: 'Ticket sent',
        ticketThanks: 'Thanks! Our team will get back to you shortly.',
      },
    },
    numberInput: {
      lead: 'A numeric spinbutton field: a text input with <code>role="spinbutton"</code> flanked by −/+ buttons, the full keyboard map (arrows, Shift+arrows, PageUp/PageDown, Home/End), clamping to <code>min</code>/<code>max</code>, precision rounding and optional <code>Intl.NumberFormat</code> display while blurred. Same color × variant API as the rest of the field family, and a <code>ControlValueAccessor</code> for template- and Reactive Forms.',
      apiCaption: 'dm-number-input inputs & outputs',
      defaultsDesc:
        'Change the defaults of every <code>dm-number-input</code> with the <code>NUMBER_INPUT_DEFAULTS</code> token or the <code>provideNumberInputDefaults</code> helper — including the built-in <code>decrementLabel</code> / <code>incrementLabel</code> ARIA copy of the −/+ buttons, which you should localize per app language:',
      a11yItems: [
        'The input is <code>role="spinbutton"</code> with <code>aria-valuemin</code> / <code>aria-valuemax</code> / <code>aria-valuenow</code>, plus <code>aria-valuetext</code> with the formatted text when <code>formatOptions</code> is set — so a screen reader announces “€1,299.50”, not the raw digits.',
        'Full keyboard map: <strong>↑/↓</strong> step by <code>step</code>, <strong>Shift+↑/↓</strong> and <strong>PageUp/PageDown</strong> step ×10, <strong>Home/End</strong> jump to <code>min</code>/<code>max</code>, <strong>Enter</strong> commits the typed value and <strong>Escape</strong> reverts uncommitted typing.',
        'The −/+ buttons carry <code>aria-label</code>s (<code>decrementLabel</code> / <code>incrementLabel</code>), are out of the tab order (<code>tabindex="-1"</code>) since the keyboard already steps from the input, disable themselves at the bounds, and support press-and-hold to repeat.',
        'With a visible <code>label</code> the input is wired via <code>aria-labelledby</code>; without one, pass <code>ariaLabel</code>. <code>required</code> sets <code>aria-required</code>.',
        '<code>error</code> sets <code>aria-invalid</code> and links the message with <code>aria-describedby</code> (the message is <code>role="alert"</code>); an external <code>&lt;dm-error&gt;</code> can be linked the same way through <code>ariaDescribedby</code>.',
        'Same focus contract as the whole field family: primary focus ring, 32/40/48px heights, <code>inputmode="decimal"</code> so mobile keyboards open the numeric pad.',
      ],
      api: {
        value: 'Two-way numeric value: [(value)] / (valueChange). null while the field is empty.',
        min: 'Lower bound (inclusive). Typed values are clamped on commit; Home jumps here.',
        max: 'Upper bound (inclusive). Typed values are clamped on commit; End jumps here.',
        step: 'Amount added/removed by the buttons and arrow keys (×10 with Shift / PageUp / PageDown).',
        precision:
          'Decimal places the committed value is rounded to. Defaults to the decimals in step (and min).',
        label: 'Visible label above the field.',
        placeholder: 'Shown while the field is empty.',
        description: 'Help text below the field (hidden while error is set).',
        error: 'Error text; non-empty activates the invalid state.',
        disabled: 'Disables the field; combined with the forms disabled state.',
        readonly: 'Read-only: value visible, buttons and stepping off.',
        required: 'Shows the * marker and sets aria-required.',
        hideControls: 'Hides the −/+ buttons. Keyboard stepping keeps working.',
        color: 'Semantic color for the focus ring and caret.',
        variant: 'Field surface: flat, bordered, faded, underlined.',
        size: 'Field height (32 / 40 / 48px).',
        radius: 'Corner rounding (md default, matching the field family; full = pill).',
        formatOptions:
          'Intl.NumberFormat options used to display the value while NOT focused (currency, percent, unit…). The model always holds the raw number.',
        locale: 'BCP-47 locale for formatOptions; runtime default when omitted.',
        name: 'name for the native input (form submission / autofill).',
        ariaLabel: 'Accessible name when no visible label.',
        ariaDescribedby: 'Extra aria-describedby id(s), e.g. an external <dm-error>.',
        decrementLabel: 'ARIA label of the − button (localize for multilingual apps).',
        incrementLabel: 'ARIA label of the + button (localize for multilingual apps).',
        valueCommit:
          'Emitted after a value is committed (blur, Enter or a step) — also fires with null.',
      },
      labels: {
        usageDesc:
          'Bind with <code>[(value)]</code> for standalone use, or drop it into a form with <code>[formControl]</code> / <code>formControlName</code> — the model is always a raw <code>number | null</code>.',
        qtyLabel: 'Quantity',
        qtyHint: 'Use the buttons or the arrow keys.',
        value: 'Value',
        lastCommit: 'Last commit',
        rangeHeading: 'Min, max & step',
        rangeLabel: 'Discount (%)',
        rangeHint: '0–100 in steps of 5 — type 250 and blur: it clamps to 100.',
        decimalsHeading: 'Decimals & precision',
        hoursLabel: 'Hours logged',
        hoursHint: 'step 0.25 → two decimals inferred.',
        weightLabel: 'Weight (kg)',
        weightHint: 'step 0.1, precision pinned to 2.',
        formatHeading: 'Formatting (Intl)',
        formatNote:
          'Formatted while blurred, raw number while focused — click into a field to see the switch. The first and last fields follow the dashboard language; the budget is pinned to <code>en-US</code>.',
        priceLabel: 'Price',
        priceHint: 'Currency, EUR — follows your locale.',
        budgetLabel: 'Budget',
        distanceLabel: 'Distance',
        keyboardHeading: 'Keyboard & hidden controls',
        yearLabel: 'Year',
        yearHint: 'No buttons — use the keyboard.',
        kbUpDown: 'One step',
        kbShift: 'Step ×10',
        kbPage: 'Step ×10',
        kbHomeEnd: 'Jump to min / max',
        kbEnter: 'Commit the typed value',
        kbEscape: 'Revert uncommitted typing',
        disabledLabel: 'Disabled',
        readonlyLabel: 'Read-only',
        requiredLabel: 'Required',
        seatsLabel: 'Seats',
        seatsError: 'Only 10 seats are available.',
        cartTitle: 'Your cart',
        cartItems: 'items',
        cartItem1: 'Wireless headphones',
        cartItem2: 'Mechanical keyboard',
        cartItem3: 'USB-C hub',
        each: 'each',
        inStock: 'in stock',
        qtyFor: 'Quantity:',
        decrease: 'Decrease quantity',
        increase: 'Increase quantity',
        onlyLeftPrefix: 'Only',
        onlyLeftSuffix: 'left in stock.',
        subtotal: 'Subtotal',
        shipping: 'Shipping',
        freeShipping: 'Free',
        total: 'Total',
        checkout: 'Checkout',
        processing: 'Processing…',
        orderPlaced: 'Order placed',
      },
    },
    emptyState: {
      lead: 'Placeholder for “there is nothing here yet”: a glyph, a short explanation and an optional call to action. Drop it inside tables, lists, search results or whole pages when the data set is empty. The built-in inbox glyph is replaced by projecting any element marked with <code>dmEmptyStateIcon</code>.',
      apiCaption: 'dm-empty-state inputs',
      defaultsDesc:
        'Change the defaults of every empty state with the <code>EMPTY_STATE_DEFAULTS</code> token or the <code>provideEmptyStateDefaults</code> helper.',
      a11yItems: [
        'The icon area is decorative (<code>aria-hidden="true"</code>): the message is carried entirely by the title and description text.',
        'No live region by design — an empty state is static content. If the emptiness results from a user action (a filter with zero matches), announce it from the consumer with <code>aria-live</code> where it matters.',
        'Projected actions are regular buttons and links: they keep their own focus order, roles and labels.',
      ],
      api: {
        title: 'Bold headline. Keep it short (“No results”).',
        description: 'Supporting copy under the title. Free content can also be projected.',
        size: 'Icon, spacing and type scale.',
        hideIcon: 'Removes the icon area entirely (built-in glyph and projected icon alike).',
      },
      labels: {
        withActions: 'With actions',
        customIcon: 'Custom icon',
        noIcon: 'Without icon',
        inContext: 'Inside a panel',
        noProjects: 'No projects yet',
        noProjectsDesc: 'Create your first project to get started.',
        newProject: 'New project',
        importProject: 'Import',
        noResults: 'No results',
        noResultsDesc: 'Try a different search term or clear the filters.',
        clearFilters: 'Clear filters',
        inboxEmpty: 'Your inbox is empty',
        inboxEmptyDesc: 'Messages you receive will show up here.',
        noNotifications: 'No notifications',
        noFiles: 'No files uploaded',
        noFilesDesc: 'Drag files anywhere in this panel to upload them.',
        documentsTitle: 'Documents',
        teamTitle: 'Team members',
        searchMembers: 'Search members…',
        invite: 'Invite member',
        role1: 'Design',
        role2: 'Engineering',
        role3: 'Product',
        noMatches: 'No members found',
        noMatchesDesc: 'Nobody matches your search. Check the spelling or invite them to the team.',
        clearSearch: 'Clear search',
      },
    },
    toggleGroup: {
      lead: 'Segmented control that groups a row of <code>dm-toggle</code> items in one flat, rounded surface. Two modes: an exclusive <strong>single</strong> choice (view switchers) with a radio-group keyboard model, or independent <strong>multiple</strong> toggles (a formatting toolbar) with <code>aria-pressed</code> buttons. Both are ControlValueAccessor, so <code>value</code> / <code>values</code> work with template-driven and Reactive Forms.',
      apiCaption: 'dm-toggle-group and dm-toggle inputs',
      defaultsDesc:
        'Change the defaults of every toggle group with the <code>TOGGLE_GROUP_DEFAULTS</code> token or the <code>provideToggleGroupDefaults</code> helper.',
      a11yItems: [
        'Single mode is a <code>role="radiogroup"</code> with <code>role="radio"</code> children (<code>aria-checked</code>): one tab stop, arrow keys move <em>and</em> select, Home/End jump to the ends.',
        'Multiple mode is a <code>role="group"</code> of <code>aria-pressed</code> buttons: each segment is reachable by Tab and toggled with Space/Enter.',
        'Icon-only segments require an <code>ariaLabel</code>. Disabled segments are skipped by the keyboard model and are never tab stops.',
        'Focus shows the shared focus ring; the selected label always meets AA contrast on its fill (white on solid colors, foreground on the neutral chip).',
      ],
      api: {
        multiple:
          'Multi-select mode: independent aria-pressed toggles instead of one exclusive choice.',
        value: 'Selected value in single mode. Two-way [(value)]. Ignored when multiple.',
        values:
          'Selected values (array) in multiple mode. Two-way [(values)]. Ignored when single.',
        color: 'Color of the selected segment(s). default keeps the neutral raised chip.',
        size: 'Control height and type scale (sm / md / lg).',
        orientation: 'Layout direction of the segments.',
        fullWidth: 'Stretch the group to fill its container; segments share the width.',
        disabled: 'Disables every segment (combined with the forms disabled state).',
        groupAriaLabel: 'Accessible label for the group when there is no visible caption.',
        toggleValue: 'Value carried by a dm-toggle; the group compares by strict equality.',
        toggleDisabled: 'Disables just this segment.',
        toggleAriaLabel: 'Accessible label of a dm-toggle — required for icon-only segments.',
      },
      labels: {
        single: 'Single (segmented control)',
        multiple: 'Multiple (toolbar)',
        colors: 'Colors',
        sizes: 'Sizes',
        fullWidth: 'Full width',
        vertical: 'Vertical',
        list: 'List',
        grid: 'Grid',
        table: 'Table',
        board: 'Board',
        bold: 'Bold',
        italic: 'Italic',
        underline: 'Underline',
        alignLeft: 'Align left',
        alignCenter: 'Align center',
        alignRight: 'Align right',
        day: 'Day',
        week: 'Week',
        month: 'Month',
        selected: 'Selected',
        none: 'none',
        dashboardTitle: 'Analytics',
        rangeLabel: 'Range',
        chartHint: 'Showing data for the selected range.',
      },
    },
    buttonGroup: {
      lead: 'Joins a row (or column) of <code>dm-button</code>s into one attached control: inner corners are flattened and shared borders collapse so the buttons read as a single segmented bar. Purely presentational — every button keeps its own <code>color</code> / <code>variant</code> / <code>size</code> and its own click handler. A <strong>split button</strong> is just a group of two: the main action plus a caret that opens a menu.',
      apiCaption: 'dm-button-group inputs',
      defaultsDesc:
        'Change the defaults of every button group with the <code>BUTTON_GROUP_DEFAULTS</code> token or the <code>provideButtonGroupDefaults</code> helper.',
      a11yItems: [
        'The host is <code>role="group"</code>; pass <code>ariaLabel</code> so assistive tech announces what the set of buttons is for.',
        'Each button stays a normal, independently focusable <code>&lt;button&gt;</code> with its own accessible name — icon-only buttons still need their own <code>ariaLabel</code>.',
        'The hovered / focused button rises above its neighbours so its border and focus ring are never clipped by the adjacent one.',
        'The split-button caret uses the same menu as <code>dm-menu</code>: full keyboard support, roving focus and <code>aria-haspopup</code>.',
      ],
      api: {
        color: 'Cascades to every button; each button may override with its own color.',
        variant: 'Cascades to every button; each button may override with its own variant.',
        size: 'Cascades to every button; each button may override with its own size.',
        radius: 'Rounding of the outer corners; cascades like the rest.',
        disabled: 'Disables every button in the group.',
        orientation: 'Layout direction of the grouped buttons.',
        fullWidth: 'Stretch the group to fill its container; buttons share the width equally.',
        groupAriaLabel: 'Accessible label describing the set of actions (e.g. "Text alignment").',
      },
      labels: {
        override: 'Per-button override',
        fileActions: 'File actions',
        rename: 'Rename',
        duplicate: 'Duplicate',
        delete: 'Delete',
        attached: 'Attached buttons',
        alignment: 'Alignment toolbar',
        pagination: 'Pager',
        vertical: 'Vertical',
        fullWidth: 'Full width',
        split: 'Split button',
        alignLabel: 'Text alignment',
        left: 'Left',
        center: 'Center',
        right: 'Right',
        justify: 'Justify',
        prev: 'Previous',
        next: 'Next',
        page: 'Page',
        bold: 'Bold',
        italic: 'Italic',
        underline: 'Underline',
        formatLabel: 'Text format',
        save: 'Save',
        moreOptions: 'More save options',
        saveClose: 'Save and close',
        saveCopy: 'Save as copy…',
        saveTemplate: 'Save as template',
        savedToast: 'Saved',
        zoomIn: 'Zoom in',
        zoomOut: 'Zoom out',
        reset: 'Reset',
        editorTitle: 'Document',
        editorHint: 'Formatting applies to the selection.',
      },
    },
    copyButton: {
      lead: 'Copies its <code>value</code> to the clipboard on click, then flips its icon from copy to a check for a moment as confirmation. A thin wrapper over <code>dm-button</code>, so it shares the same <code>color</code> × <code>variant</code> × <code>size</code> × <code>radius</code> and presses, focuses and themes identically. Under the hood it is driven by the <code>dmCopyToClipboard</code> directive, which you can attach to any button you already have.',
      apiCaption: 'dm-copy-button inputs',
      defaultsDesc:
        'Change the defaults of every copy button with the <code>COPY_BUTTON_DEFAULTS</code> token or the <code>provideCopyButtonDefaults</code> helper.',
      a11yItems: [
        'The copy / check glyph is <code>aria-hidden</code>; the accessible name comes from <code>ariaLabel</code> (icon-only) or the visible <code>copyLabel</code>.',
        'On a successful copy a visually-hidden <code>role="status"</code> live region announces <code>copiedAriaLabel</code>, so screen-reader users get confirmation.',
        'Keyboard focus, the focus ring and the elastic press are inherited from the underlying <code>dm-button</code> — it stays a real <code>&lt;button&gt;</code>.',
        'The composing <code>dmCopyToClipboard</code> directive is SSR-safe (a no-op on the server) and adds the same copy behaviour to any element you already have.',
      ],
      api: {
        value: 'Text written to the clipboard on click. Required.',
        color: 'Semantic color, forwarded to the inner dm-button.',
        variant: 'Visual variant, forwarded to the inner dm-button.',
        size: 'Control size, forwarded to the inner dm-button.',
        radius: 'Corner rounding, forwarded to the inner dm-button.',
        resetDelay: 'How long the copied (check) state lasts before reverting, in milliseconds.',
        copyLabel: 'Optional visible label beside the icon. Empty = icon-only.',
        copiedLabel: 'Visible label shown while copied. Falls back to copyLabel.',
        ariaLabel: 'Accessible name for the button — required for the icon-only form.',
        copiedAriaLabel: 'Announced to screen readers after a successful copy.',
        copied: 'Emitted with the copied text after a successful copy.',
        copyError: 'Emitted when the clipboard write fails (denied, insecure context, SSR).',
        directive:
          'Companion [dmCopyToClipboard] directive: adds copy behaviour to any element and exposes an isCopied() signal via #ref="dmCopyToClipboard".',
        disabled: 'Disables the button; copying is blocked while disabled.',
        copyIconSlot:
          'Projected [dmCopyIcon] element replacing the built-in copy glyph while idle.',
        copiedIconSlot:
          'Projected [dmCopiedIcon] element replacing the built-in check glyph while copied.',
      },
      labels: {
        variantsHeading: 'Colors and variants',
        directiveHeading: 'The dmCopyToClipboard directive',
        installValue: 'dm_a1B2c3D4e5',
        copyCommand: 'Copy API key',
        copiedCommand: 'API key copied',
        copyText: 'Copy',
        copiedText: 'Copied!',
        copyValue: 'Copy value',
        copiedValue: 'Value copied',
        shareLabel: 'Share link',
        shareValue: 'https://dmasterui.com',
        couponLabel: 'Coupon code',
        directiveValue: 'WELCOME25',
        cardTitle: 'API credentials',
        cardHint: 'Keep these secret. Copy them straight into your .env file.',
        apiKey: 'sk_test_dmaster_ui_1a2b3c4d',
        copyKey: 'Copy API key',
        copiedKey: 'API key copied',
        copiedToast: 'Copied to clipboard',
      },
    },
    otp: {
      lead: 'One-time-code / PIN field: a row of single-character cells that behaves as one control. Typing advances to the next cell, Backspace clears and steps back, arrows move between cells and a paste is distributed across them. A ControlValueAccessor, so <code>[(value)]</code> works standalone and <code>[formControl]</code> in Reactive Forms; <code>(completed)</code> fires once every cell is filled.',
      apiCaption: 'dm-otp inputs and outputs',
      defaultsDesc:
        'Change the defaults of every OTP field with the <code>OTP_DEFAULTS</code> token or the <code>provideOtpDefaults</code> helper.',
      a11yItems: [
        'The host is <code>role="group"</code> with your <code>ariaLabel</code>; each cell is a labelled <code>&lt;input&gt;</code> announced as "{label} N of M".',
        '<code>autocomplete="one-time-code"</code> lets the platform offer an SMS code on the first cell; a paste of the full code spreads across the remaining cells.',
        'Positions are preserved — clearing a middle cell never shifts the trailing ones. <code>mode</code> sets <code>inputmode</code> so mobiles show the right keyboard.',
        'The focus ring uses the shared field-family language (<code>default</code> focuses primary), and masked mode renders each filled cell as a password dot.',
      ],
      api: {
        length: 'Number of cells.',
        value: 'The code. Two-way [(value)].',
        mode: 'Accepted characters and the mobile keyboard: numeric, alphanumeric or text.',
        variant: 'Cell surface: flat (muted), bordered (elevated + border), faded, underlined.',
        groupSize: 'Inserts a separator after every N cells (0 = none): 123 – 456.',
        mask: 'Render filled cells as masked dots (like a password).',
        size: 'Cell size (sm / md / lg).',
        color: 'Focus-ring color; default focuses primary like the rest of the field family.',
        disabled: 'Disables every cell (combined with the forms disabled state).',
        autoFocus: 'Focus the first cell on init.',
        ariaLabel: 'Accessible label of the group and the per-cell prefix.',
        completed: 'Fires once, with the full code, when the last empty cell is filled.',
        label: 'Visible label above the cells; names the role=group wrapper via aria-labelledby.',
        description: 'Help text under the cells, hidden while an error is set.',
        error:
          'Error text; a non-empty value activates the invalid state and is announced with role=alert.',
        readonly:
          'Read-only: the code stays visible and focusable, but edits (typing, paste) are blocked.',
        required: 'Shows the required marker next to the label.',
      },
      labels: {
        numeric: 'Numeric (default)',
        alphanumeric: 'Alphanumeric',
        masked: 'Masked PIN',
        sizes: 'Sizes',
        separated: 'Grouped with a separator',
        fourDigit: 'Four digits',
        code: 'Verification code',
        pin: 'PIN',
        current: 'Value',
        verifyTitle: 'Verify your email',
        verifyHint: 'Enter the 6-digit code we sent to a•••@example.com.',
        verify: 'Verify',
        resend: 'Resend code',
        verifying: 'Verifying…',
        verified: 'Verified',
        invalid: 'That code is not valid.',
        completeMsg: 'Code complete',
      },
    },
  },
};
